# 开发过程记录

> **日期**: 2026-06-01
> **任务编号**: B1
> **所属模块**: agent-service
> **前置依赖**: API 契约文档 ✅

---

## 一、背景

Agent 服务的原架构通过 `httpx` 调用 Claude API（`api.anthropic.com`）和 OpenAI API（`api.openai.com`）来完成对话。这种架构存在三个核心问题：

1. **成本不可控**：每次对话都消耗云端 API 额度，大规模使用时费用高昂。
2. **网络依赖**：需要稳定的外网连接，内网/离线环境无法使用。
3. **可扩展性差**：DeepSeek 无官方本地 CLI，适配器存在但不能统一方案。

本次重构将 Agent 服务从「远程 HTTP API 调用」切换为「本地 CLI 子进程调用」，消除云端依赖，使项目可以在完全离线的环境中运行。

---

## 二、任务执行过程

### 2.1 适配器重构：HTTP API → 本地 CLI

**操作内容**：

**Claude 适配器** (`agent-service/adapters/claude_adapter.py`)：
```python
# 原方式：httpx 异步 HTTP 调用
# async with httpx.AsyncClient() as client:
#     response = await client.post("https://api.anthropic.com/v1/messages", ...)

# 新方式：本地 CLI 子进程
process = await asyncio.create_subprocess_exec(
    resolved_command, "-p", prompt,
    stdout=asyncio.subprocess.PIPE,
    stderr=asyncio.subprocess.PIPE
)
```

**Codex 适配器** (`agent-service/adapters/codex_adapter.py`)：
```python
# CLI 命令: codex exec "<prompt>"
process = await asyncio.create_subprocess_exec(
    resolved_command, "exec", prompt,
    stdout=asyncio.subprocess.PIPE,
    stderr=asyncio.subprocess.PIPE
)
```

**关键逻辑**：
- `_build_prompt()` 拼接 system_prompt + history + message
- 逐行读取 stdout，`strip_ansi()` 剥离 ANSI 颜色码
- 后台读取 stderr 避免管道阻塞
- 超时控制（`asyncio.wait_for`），非零退出码返回 stderr 尾部 500 字
- CLI 未安装时返回友好错误提示

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 调用方式 | `asyncio.create_subprocess_exec` | 原生异步，无需额外依赖，与 FastAPI 事件循环天然兼容 |
| stderr 处理 | 后台 Task 异步读取 | 避免管道缓冲区写满导致子进程卡死 |
| ANSI 剥离 | `strip_ansi()` | CLI 输出包含终端颜色码，影响下游解析 |

### 2.2 配置层更新

**config.py**：
- 新增 `CLAUDE_CLI_COMMAND`、`CLAUDE_CLI_ARGS`、`CODEX_CLI_COMMAND`、`CODEX_CLI_ARGS`、`AGENT_WORKING_DIRECTORY`
- 删除所有 API 相关配置项（`CLAUDE_API_KEY`、`CLAUDE_API_URL`、`CODEX_API_KEY` 等）
- 超时从 120s 调整到 300s（CLI 执行任务更耗时）

**models.py**：
- `AgentChatRequest` 新增 `workingDirectory: Optional[str]` 字段
- 新增 Response Models：`AgentChatResponse`、`HealthResponse`、`ErrorResponse`，全部带 `Field(description=, examples=)`

### 2.3 Windows 兼容性修复

**问题一：CLI 命令找不到（FileNotFoundError）**

| 项目 | 内容 |
|------|------|
| 现象 | `asyncio.create_subprocess_exec("claude")` 抛出 `FileNotFoundError` |
| 根因 | Windows 上 npm 全局安装的是 `.cmd` 文件（如 `claude.CMD`），底层 `CreateProcess` 不会自动补全扩展名 |
| 修复 | 引入 `shutil.which()` 预解析完整路径：`resolved_command = shutil.which(self.cli_command)` |

**问题二：工作目录无效（WinError 267）**

| 项目 | 内容 |
|------|------|
| 现象 | Swagger example 值 `/path/to/project` 被发送，但它是 truthy 字符串，`or` 不回退 |
| 修复 | 用 `os.path.isdir()` 做实打实的路径存在性校验 |

```python
wd = kwargs.get("working_directory")
if not wd or not os.path.isdir(wd):
    wd = self.default_cwd
```

### 2.4 接口规范化

对齐 API 契约文档第 4、5 节：

- **`/health`**：新增 `uptime` 字段
- **`/api/agent/chat`**：新增非流式模式（`stream=false` 返回 JSON），默认改为非流式
- **错误响应**：统一格式 `{"error":"CODE","message":"...","timestamp":"...","path":"..."}`
  - 400 → `VALIDATION_ERROR`
  - 404 → `NOT_FOUND`
  - 500 → `INTERNAL_ERROR`
  - Agent 调用失败 → `502 AGENT_ERROR`
  - 原 422 转为 400

### 2.5 Swagger 文档更新

- FastAPI 应用元数据：`description`、`openapi_tags`、`contact`、`license_info`
- 各端点添加 `summary`、`description`、`responses` 示例
- 移除 `/chat` 端点的 `response_model`（SSE/JSON 双模式不能用单一 model 描述）
- `_custom_openapi()` 遍历删除自动注入的 422

### 2.6 清除 DeepSeek 适配器

**已删除** `agent-service/adapters/deepseek_adapter.py`，原因：无官方本地 CLI。

从以下文件彻底清理引用：
- `config.py`：删除 4 个配置项
- `adapter_factory.py`：删除导入和类型映射
- `messages.py`：移除 `AGENT_DISPLAY_NAMES` 中的类型
- `main.py`：移除 `description` 中的 DeepSeek 文本
- `__init__.py`：更新包文档
- `models.py`：默认 `agentType` 改为 `"claude_code"`

---

## 三、技术要点讲解

### 3.1 `asyncio.create_subprocess_exec` 的正确用法

```python
# 错误：直接传 CLI 命令名（Windows 上会 FileNotFoundError）
await asyncio.create_subprocess_exec("claude", "-p", prompt)

# 正确：先用 shutil.which() 解析完整路径
resolved = shutil.which("claude")  # → "E:\nodejs\claude.CMD"
await asyncio.create_subprocess_exec(resolved, "-p", prompt)
```

### 3.2 stderr 管道阻塞问题

子进程的 stderr 管道有固定缓冲区。如果主进程不读取 stderr 而子进程大量输出到 stderr，缓冲区写满后子进程会卡死。解决方案：启动一个后台 Task 持续读取。

```python
async def read_stderr():
    async for line in process.stderr:
        stderr_lines.append(line.decode())
stderr_task = asyncio.create_task(read_stderr())
```

### 3.3 truthy 字符串陷阱

```python
# 陷阱代码：任何非空字符串都是 truthy
wd = request.working_directory or default_cwd
# 当 request.working_directory = "/path/to/project"（Swagger example 值）
# 用户没有修改，但 "/path/to/project" 是 truthy，or 不会取 default_cwd

# 正确写法：
wd = request.working_directory
if not wd or not os.path.isdir(wd):
    wd = default_cwd
```

### 3.4 FastAPI 双响应模式的 OpenAPI 处理

`/api/agent/chat` 在 `stream=true` 时返回 SSE，`stream=false` 时返回 JSON。FastAPI 的 `response_model` 只能声明一种类型，所以改用 `responses` dict 手动描述两种响应格式。同时 `_custom_openapi()` 遍历删除 FastAPI 自动注入的 422 响应。

---

## 四、验证测试

### 4.1 自动化验证

| 测试项 | 命令 | 结果 |
|--------|------|------|
| Python 语法检查 | `python -m py_compile agent-service/adapters/claude_adapter.py` | |
| Python 语法检查 | `python -m py_compile agent-service/adapters/codex_adapter.py` | |
| 导入检查 | `python -c "from agent_service.main import app"` | |
| FastAPI 服务启动 | `uvicorn agent-service.main:app --reload` | |

### 4.2 手动功能测试清单

| # | 测试项 | 预期行为 | 结果 |
|---|--------|---------|------|
| 1 | /health 端点 | 返回 `{"status":"ok","version":"...","uptime":...}` | |
| 2 | /api/agent/chat 非流式 (claude_code) | 返回 JSON `{"content":"...","messageId":"..."}` | |
| 3 | /api/agent/chat 非流式 (codex) | 同上 | |
| 4 | /api/agent/chat 流式 (stream=true) | SSE 逐 token 推送 | |
| 5 | CLI 未安装场景 | 返回友好错误提示 | |
| 6 | 非法 agentType | 返回 400 VALIDATION_ERROR | |
| 7 | Agent 执行超时 | 返回 502 AGENT_ERROR | |
| 8 | Swagger UI 可访问 | `/docs` 正常显示，422 不存在 | |

---

## 五、关键决策记录

| # | 决策 | 结论 | 理由 |
|---|------|------|------|
| 1 | Agent 调用架构 | HTTP API → 本地 CLI 子进程 | 消除云端依赖和 API 费用，支持离线运行 |
| 2 | CLI 命令解析 | `shutil.which()` 预解析 | Windows 上 `CreateProcess` 不会自动补全 `.cmd` 扩展名 |
| 3 | 工作目录校验 | `os.path.isdir()` 实打实检查 | truthy 字符串陷阱，不能依赖 `or` 回退 |
| 4 | 超时策略 | 120s → 300s | CLI 执行代码生成等任务比 API 调用耗时长 |
| 5 | DeepSeek 适配器 | 删除 | 无官方本地 CLI，无法统一到子进程架构 |
| 6 | 流式默认值 | `stream` 默认 `false` | 方便 Swagger 直接查看 JSON 响应 |
| 7 | 422 错误码 | 转为 400 | 与 API 契约文档统一错误格式对齐 |

---

## 六、产物清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `agent-service/adapters/claude_adapter.py` | 重写 | HTTP 调用改为 CLI 子进程 |
| `agent-service/adapters/codex_adapter.py` | 重写 | HTTP 调用改为 CLI 子进程 |
| `agent-service/adapters/deepseek_adapter.py` | 已删除 | 无官方本地 CLI |
| `agent-service/adapters/base_adapter.py` | 未修改 | 接口契约保持不变 |
| `agent-service/adapters/__init__.py` | 修改 | 更新包文档 |
| `agent-service/adapters/adapter_factory.py` | 修改 | 更新类型映射，移除 DeepSeek |
| `agent-service/config.py` | 修改 | 新增 CLI 配置，删除 API 配置 |
| `agent-service/models.py` | 修改 | 新增 Response Models，修改默认 agentType |
| `agent-service/main.py` | 修改 | /health 增 uptime，错误处理器，Swagger 元数据 |
| `agent-service/app/api/endpoints/messages.py` | 修改 | 非流式模式，错误格式，移除 DeepSeek |
| `agent_collaboration/program.md` | 修改 | 重构为开发记录格式 |

### 代码统计

| 指标 | 数值 |
|------|------|
| 新增文件 | 0 |
| 修改文件 | 8 |
| 删除文件 | 1 |
| 重写文件 | 2 |

---

## 七、数据流/架构影响

### 变化前（HTTP API 架构）

```
用户请求 → FastAPI → Adapter → httpx → 云端 API (api.anthropic.com / api.openai.com)
                                              ↓
                                          返回 JSON → 流式推送 SSE
```

### 变化后（本地 CLI 架构）

```
用户请求 → FastAPI → Adapter → asyncio.create_subprocess_exec → 本地 CLI (claude / codex)
                                                                      ↓
                                                                  逐行 stdout → 流式推送 SSE
```

**核心变化**：网络边界从「出站 HTTPS」收缩为「本机进程间通信」，延迟从网络 RTT 降低为进程启动开销。

---

## 八、后续任务依赖

| 任务 | 可开始时机 | 关联 |
|------|----------|------|
| 本地 CLI 环境安装文档 | 立即 | 开发环境需要安装 claude / codex CLI |
| CI/CD 流水线适配 | 立即 | 构建环境需预装 CLI 工具 |
| 前端 Agent 选择器更新 | 立即 | 移除 DeepSeek 选项，Agent 类型列表已变更 |
