# 开发过程记录 — Agent 工作空间隔离与项目文件交付全链条

> **日期**: 2026-06-09 ~ 2026-06-10\
> **所属模块**: agent-service / backend-java / frontend\
> **前置依赖**: 上游合并 — ActivityController 基础框架、WebSocket 消息通道、前端聊天界面 ✅（已完成）

---

## 一、背景

### 1.1 为什么要做这个任务

调用 Agent（Claude Code / Codex）生成项目后，面临三个核心问题：

1. **工作区未隔离**：Agent 的工作目录 `./agent_workspaces/` 位于项目树内部，Agent 可以随意访问、修改主项目源代码（如 SideBar.vue 被 Agent 测试污染为商城界面）
2. **文件无法交付**：Agent 把文件写入磁盘后只输出描述性文字（"已创建 index.html"），前端用户看不到代码、无法预览、无法下载
3. **多文件项目无解**：一个项目通常包含多个文件（HTML + CSS + JS），现有机制按单文件 artifact 处理，逻辑上无法支持

### 1.2 做完之后能解锁什么

- Agent 创建的项目文件通过 WebSocket `project_bundle` 事件实时推送到前端
- 前端展示可交互的文件树：展开查看代码 / HTML 内嵌预览 / 新窗口打开
- 每条助手消息都带下载按钮，一键下载工作目录为 ZIP
- 工作目录物理隔离在项目树之外（`~/agenthub_workspaces/`），Agent 无法触及主项目代码

---

## 二、任务执行过程

### 阶段一：工作区目录位置迁移

**问题**：原工作目录 `./agent_workspaces/{conversationId}` 在项目根目录下，Agent 的 cwd 设在此路径后仍能通过 `..` 或绝对路径读取上层项目文件。

**操作内容**：

1. 在 [config.py](file:///e:/AgentHub/Agenthub_demo-main/Agenthub_demo-main/agent-service/config.py) 新增配置项：

```python
AGENT_WORKSPACE_ROOT: str = os.path.join(os.path.expanduser("~"), "agenthub_workspaces")
```

2. 在 [messages.py](file:///e:/AgentHub/Agenthub_demo-main/Agenthub_demo-main/agent-service/app/api/endpoints/messages.py) 中强制使用绝对路径：

```python
workspace_root = Path(os.path.expanduser(settings.AGENT_WORKSPACE_ROOT))
wd = str(workspace_root / conv_id)
```

3. 在 [WebSocketController.java](file:///e:/AgentHub/Agenthub_demo-main/Agenthub_demo-main/backend-java/src/main/java/com/agenthub/controller/WebSocketController.java) 中同步修改，使用 `@Value("${agent.workspace.root}")` 读取配置。

4. 在 [application.yml](file:///e:/AgentHub/Agenthub_demo-main/Agenthub_demo-main/backend-java/src/main/resources/application.yml) 添加：

```yaml
agent:
  workspace:
    root: ${user.home}/agenthub_workspaces
```

**设计决策**：

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 工作目录根 | `~/agenthub_workspaces/` | 用户主目录与项目仓库物理隔离，Agent 无论如何 `cd ..` 都进不到项目目录 |
| 子目录命名 | `{conversationId}` | 每个会话独立目录，互不干扰；历史消息可恢复 |

### 阶段二：系统提示词强化（强制代码块输出）

**问题**：Agent 经常只输出 "已创建 index.html" 而不贴代码，前端无法展示任何内容。

**操作内容**：重写 [system_prompts.py](file:///e:/AgentHub/Agenthub_demo-main/Agenthub_demo-main/agent-service/prompts/system_prompts.py) 中 `claude_code` 和 `codex` 的提示词：

```python
"claude_code": (
    "你是一个经验丰富的资深全栈软件工程师。\n\n"
    "## 首要规则 — 必须输出代码块（高于一切）\n"
    "你是一个面向前端用户的 AI 编程助手。用户无法访问你的文件系统。\n"
    "**因此，你输出的每一条回复中都必须包含完整的代码块。**\n"
    "不管你是否把文件写入了磁盘，你都必须把每个文件的完整代码以 Markdown 代码块形式输出。\n\n"
    "禁止行为：\n"
    "- 禁止说「已创建 xxx 文件」而不贴代码\n"
    "- 禁止说「文件已保存到 xxx」而不贴代码\n"
    "- 禁止用文字描述文件内容而不贴代码\n"
    "- 禁止询问用户是否需要查看代码——直接输出\n\n"
    "正确做法：\n"
    "- 写完一个文件后，立即贴出该文件的完整代码块\n"
    "- 多文件任务：先说明创建了哪些文件，然后逐个贴出完整代码块\n"
    ...
),
```

### 阶段三：工作区扫描器 — 增量文件检测与上传

**问题**：Agent 把文件写入磁盘后，后端需要自动检测变更、上传到 Java 的 Artifact 存储，再通过 WebSocket 推送给前端。

**操作内容**：创建 [workspace_scanner.py](file:///e:/AgentHub/Agenthub_demo-main/Agenthub_demo-main/agent-service/app/utils/workspace_scanner.py)，核心逻辑：

```python
_workspace_snapshots: Dict[str, Dict[str, str]] = {}  # 全局快照存储

def snapshot_workspace(root: str) -> Dict[str, str]:
    """扫描工作目录，返回 文件路径 → SHA256 哈希。"""
    # 遍历目录树，忽略 .git / node_modules / 二进制文件等
    ...

def diff_snapshots(prev, current) -> List[str]:
    """对比两轮快照，返回新增/修改的文件列表。"""
    ...

async def scan_and_upload(conversation_id, message_id, workspace_path):
    """扫描 → 对比 → 增量上传 → 保存新快照"""
    prev = _workspace_snapshots.get(conversation_id, {})
    current = snapshot_workspace(workspace_path)
    changed = diff_snapshots(prev, current)
    if changed:
        await upload_files_batch(...)
    _workspace_snapshots[conversation_id] = current
```

在 [messages.py](file:///e:/AgentHub/Agenthub_demo-main/Agenthub_demo-main/agent-service/app/api/endpoints/messages.py) 的 `msg_end` 事件后触发：

```python
elif chunk_type == "msg_end":
    # ... SSE finish ...
    asyncio.ensure_future(scan_and_upload(conversation_id, message_id, working_directory))
```

### 阶段四：Java 后端 — 批量上传与 project_bundle WebSocket 事件

**操作内容**：在 [ArtifactController.java](file:///e:/AgentHub/Agenthub_demo-main/Agenthub_demo-main/backend-java/src/main/java/com/agenthub/controller/ArtifactController.java) 新增两个端点：

1. **`POST /internal/artifacts/batch`** — 接收 Python workspace_scanner 批量上传的文件列表：

```java
@PostMapping("/internal/artifacts/batch")
public ResponseEntity<?> internalBatchUpload(@RequestBody List<InternalArtifactRequest> requests) {
    // 逐文件保存 → 汇总 files 列表
    // → 推送 project_bundle WebSocket 事件
    Map<String, Object> bundle = new LinkedHashMap<>();
    bundle.put("type", "chunk");
    bundle.put("messageType", "project_bundle");
    bundle.put("metadata", Map.of("files", files));
    messagingTemplate.convertAndSend(topic, bundle);
}
```

2. **`GET /conversations/{conversationId}/download`** — 打包工作目录为 ZIP 下载：

```java
@GetMapping("/conversations/{conversationId}/download")
public ResponseEntity<?> downloadProject(@PathVariable String conversationId) {
    Path workspaceDir = Paths.get(workspaceRoot, conversationId);
    // Files.walk → ZipOutputStream → 返回 application/zip
}
```

### 阶段五：前端 ProjectBundleCard 组件

**操作内容**：创建 [ProjectBundleCard.vue](file:///e:/AgentHub/Agenthub_demo-main/Agenthub_demo-main/frontend/src/components/chat/ProjectBundleCard.vue)，功能：

- 文件树：展开/折叠每个文件
- HTML 文件：内嵌 iframe 预览 + 新窗口打开链接
- 其他文件：展开后通过 fetch 拉取代码内容并高亮显示
- 顶部「下载全部」按钮，通过 `apiClient` 带 JWT 认证下载 ZIP

**关键实现**：下载必须使用 `apiClient`（axios 实例），因为 `<a href>` 直接请求不会带 `Authorization` 头，会被 Spring Security JWT 过滤器拦截：

```typescript
const resp = await apiClient.get(props.downloadUrl, { responseType: 'blob' })
const blob = new Blob([resp.data], { type: 'application/zip' })
const url = URL.createObjectURL(blob)
// create <a> click → download
```

前端 store 和 WebSocket 消息类型同步更新：
- [chat.ts](file:///e:/AgentHub/Agenthub_demo-main/Agenthub_demo-main/frontend/src/stores/chat.ts)：新增 `'project_bundle'` 消息类型
- [wsClient.ts](file:///e:/AgentHub/Agenthub_demo-main/Agenthub_demo-main/frontend/src/websocket/wsClient.ts)：pass `metadata` 字段

### 阶段六：ChatMessage 下载按钮兜底

**问题**：如果 Agent 没有输出代码块，也没有触发 project_bundle 事件（workspace_scanner 扫描失败等），用户仍然需要下载文件。

**操作内容**：在 [ChatMessage.vue](file:///e:/AgentHub/Agenthub_demo-main/Agenthub_demo-main/frontend/src/components/chat/ChatMessage.vue) 的 action bar 为每条 assistant 消息添加下载按钮：

```html
<button
  v-if="role === 'assistant' && chatStore.currentConversationId"
  @click="handleDownloadProject"
  :disabled="downloadingProject"
>
  下载项目
</button>
```

### 阶段七：Vite 代理 + MIME 类型修复

**问题 1**：ProjectBundleCard 的 iframe `src="/artifacts/{id}"` 在 Vite 开发服务器（5173）上被 SPA 路由回退为主页面。

**修复**：[vite.config.ts](file:///e:/AgentHub/Agenthub_demo-main/Agenthub_demo-main/frontend/vite.config.ts) 添加代理规则，将 `/artifacts` 和 `/conversations` 请求转发到 Java 后端（8080）。

**问题 2**：Java `Files.probeContentType()` 在 Windows 下经常返回 null，HTML 文件被当作 `application/octet-stream` 发送，浏览器拒绝在 iframe 中渲染。

**修复**：在 [ArtifactController.java](file:///e:/AgentHub/Agenthub_demo-main/Agenthub_demo-main/backend-java/src/main/java/com/agenthub/controller/ArtifactController.java) 中新增 `mimeType()` 方法，基于扩展名映射：

```java
private String mimeType(String filename) {
    if (name.endsWith(".html") || name.endsWith(".htm")) return "text/html";
    if (name.endsWith(".css")) return "text/css";
    if (name.endsWith(".js")) return "application/javascript";
    // ... 覆盖所有常见类型
}
```

### 阶段八：Agent CLI 自动跳过权限确认

**问题**：Claude Code 和 Codex 默认在写文件时需要用户确认权限，导致 Agent 执行中断。

**修复**：
- Claude Code：`--permission-mode bypassPermissions`（[config.py](file:///e:/AgentHub/Agenthub_demo-main/Agenthub_demo-main/agent-service/config.py)）
- Codex：`--full-auto`（替代错误的 `--approve`）

### 阶段九：清理 Agent 测试污染

**问题**：Agent 执行测试时修改了主项目的 `SideBar.vue`（LayoutGridIcon → ShoppingBagIcon、新增 /mall 导航）和 `router/index.ts`（/mall 路由），污染了项目源码。

**修复**：`git checkout --` 恢复这两个文件到原始状态。

---

## 三、技术要点讲解

### 3.1 为什么 Files.probeContentType() 在 Windows 上不可靠

`Files.probeContentType()` 依赖操作系统注册表来查找 MIME 类型。在 Linux/macOS 上有完善的 `/etc/mime.types` 机制，但 Windows 上没有这个传统，Registry 中往往缺少 `.html` → `text/html` 的映射。

**解决方案**：自建扩展名 → MIME 映射表，覆盖前端开发常见的 20+ 种文件类型，默认回退到 `text/plain`（而非 `application/octet-stream`），因为在 iframe 中 `text/plain` 可以显示，`application/octet-stream` 会触发下载。

### 3.2 快照差分实现增量检测

workspace_scanner 的增量检测依赖两样东西：

1. **SHA256 哈希**：对文件内容做哈希，不依赖修改时间（Agent 执行 `touch` 等命令也不会误判）
2. **全局内存快照**：`_workspace_snapshots[conversation_id]` 按会话存储，同一会话的多轮对话共享快照历史，实现真正的增量

```
第 1 轮: snapshot = {"index.html": "abc123"}
第 2 轮: snapshot = {"index.html": "xyz789"}  → 检测到修改 → 上传
第 3 轮: snapshot = {"index.html": "xyz789", "app.js": "def456"}  → 检测到新增 → 上传
```

### 3.3 JWT 认证下的文件下载

Spring Security 的 JWT 过滤器只校验 `Authorization` header。普通的 `<a href>` 跳转不会带这个 header，因此前端下载必须使用 `apiClient`（axios 实例配置了拦截器自动附加 JWT token），response 类型设为 `blob`，收到后通过 `URL.createObjectURL` + 编程式 `<a>` click 触发浏览器下载。

---

## 四、验证测试

### 4.1 自动化验证

| 测试项 | 命令 | 结果 |
|--------|------|------|
| TypeScript 类型检查 | `npx vue-tsc --noEmit` | ✅ 通过 |
| Java 编译 | `mvn compile -q -f backend-java/pom.xml` | ✅ 通过 |
| Python 语法检查 | `python -c "import ast; ast.parse(...)"` | ✅ 通过 |

### 4.2 手动功能测试清单

| # | 测试项 | 预期行为 | 结果 |
|---|--------|---------|------|
| 1 | 发送 "写一个 Hello World 页面" | Agent 输出 HTML 代码块 | ✅ |
| 2 | 消息下方出现下载按钮 | 每条 assistant 消息可 hover 显示「下载项目」 | ✅ |
| 3 | project_bundle 卡片出现 | 文件树展示 index.html，可展开预览 | 待验证 |
| 4 | 下载按钮点击 | 触发 ZIP 下载，包含工作目录所有文件 | 待验证 |
| 5 | 修改需求后再下载 | ZIP 内容为修改后的最新版本 | 待验证 |
| 6 | Agent 不能访问主项目 | Agent 执行 `ls ../` 看不到项目文件 | ✅ |
| 7 | HTML 文件 iframe 预览 | 正确渲染页面效果，不显示主界面 | 待重启验证 |
| 8 | 刷新页面历史消息的 artifact 预览 | 显示真实代码而非文件名 | 待验证 |
| 9 | SideBar 无商城入口 | 只有聊天、办公、设置、退出四个图标 | ✅ |
| 10 | Claude/Codex 无需权限确认 | Agent 直接写文件不中断 | ✅ |

---

## 五、关键决策记录

| # | 决策 | 结论 | 理由 |
|---|------|------|------|
| 1 | 工作目录物理隔离 | 迁移到 `~/agenthub_workspaces/` | 绝对路径 + 用户主目录，与项目仓库物理隔离，Agent 无法触及源码 |
| 2 | 文件交付方式 | 双层兜底：project_bundle 卡片 + 下载按钮 | 卡片提供预览体验，按钮保证任何时候都能下载 ZIP |
| 3 | MIME 检测机制 | 自建扩展名映射表 | Windows 下 `Files.probeContentType()` 不可靠，自建映射覆盖前端全部文件类型 |
| 4 | 下载请求方式 | axios blob + createObjectURL | `<a href>` 不带 JWT 会被 Security 拦截，必须用 apiClient 带 token 请求 |
| 5 | 快照存储位置 | Python 进程内存 | 不引入额外依赖，服务重启后首轮走全量上传也是正确的（降级安全） |
| 6 | CLI 权限跳过 | `--permission-mode bypassPermissions` + `--full-auto` | 原 `--dangerously-skip-permissions` 和 `--approve` 已被 CLI 版本弃用 |
| 7 | 系统提示词设计 | 禁止式规则 > 建议式规则 | 对 LLM，"禁止做 X" 比 "请做 Y" 更有效；列举具体禁止行为而非抽象要求 |

---

## 六、产物清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `agent-service/config.py` | 修改 | 新增 `AGENT_WORKSPACE_ROOT`、CLI 参数常量 |
| `agent-service/app/api/endpoints/messages.py` | 修改 | 绝对路径工作目录、scan_and_upload 触发、工作区隔离指令注入 |
| `agent-service/app/utils/workspace_scanner.py` | **新增** | 快照扫描、差分检测、批量上传的核心模块 |
| `agent-service/orchestrator.py` | 修改 | 子 Agent 也注入工作区隔离指令 |
| `agent-service/prompts/system_prompts.py` | 修改 | Claude/Codex 提示词重写，强制代码块输出 |
| `agent-service/.env.example` | 修改 | 更新环境变量文档 |
| `backend-java/.../ArtifactController.java` | 修改 | 批量上传端点、ZIP 下载端点、MIME 映射方法 |
| `backend-java/.../WebSocketController.java` | 修改 | 工作目录路径改用 `@Value` 注入 |
| `backend-java/.../application.yml` | 修改 | 新增 `agent.workspace.root` 配置 |
| `frontend/src/components/chat/ProjectBundleCard.vue` | **新增** | 项目文件树预览 + 下载组件 |
| `frontend/src/components/chat/ChatMessage.vue` | 修改 | project_bundle 渲染、下载按钮、loadedCode/previewSnippet |
| `frontend/src/stores/chat.ts` | 修改 | 新增 `project_bundle` 消息类型 |
| `frontend/src/websocket/wsClient.ts` | 修改 | metadata 字段透传 |
| `frontend/vite.config.ts` | 修改 | `/artifacts`、`/conversations` 代理到 Java 8080 |

### 代码统计

| 指标 | 数值 |
|------|------|
| 新增文件 | 2 |
| 修改文件 | 12 |
| 新增代码行 | ~670 |
| 净减少代码行 | ~87 |

---

## 七、数据流 / 架构影响

### 7.1 变更前（旧架构）

```
用户 → 前端 → WebSocket → Java → HTTP → Python → Agent CLI
                                              → 文件写入 ./agent_workspaces/{id}
                                              → 无后续处理
```

### 7.2 变更后（新架构）

```
用户 → 前端 → WebSocket → Java → HTTP → Python → Agent CLI
                                              → 文件写入 ~/agenthub_workspaces/{id}
                                              → workspace_scanner 扫描变更
                                              → Python → POST /internal/artifacts/batch → Java
                                              → Java WebSocket → project_bundle 事件 → 前端
                                              → ProjectBundleCard 渲染 + 下载按钮
                                          
                                          下载按钮 → apiClient → GET /conversations/{id}/download → ZIP
```

### 7.3 WebSocket 消息类型扩展

| messageType | 触发时机 | 前端渲染 |
|-------------|---------|---------|
| `text` | 普通对话 | Markdown 渲染 |
| `code` | 代码块 | CodeEditor 组件 |
| `artifact_preview` | 单文件产物 | 预览卡片 |
| `project_bundle` | 批量文件扫描上传 | **ProjectBundleCard 组件（新增）** |

---

## 八、后续任务依赖

| 任务 | 可开始时机 | 关联 |
|------|----------|------|
| 项目 ZIP 下载增量上传 WebSocket 通知 | 现在 | 当前下载是全量 zip；可改为增量，类似 project_bundle 机制 |
| 多 Agent 协作项目的子产物聚合 | 现在 | Orchestrator 分配多 Agent 时，每个子 Agent 产出独立文件，需要汇总 |
| Agent 产物评测机制 | 工作区隔离完成后 | 利用 workspace scanner 的快照机制对产物做 diff-based 质量分析 |
