# 开发过程记录 — 项目进度评审与 P1 阶段规划

> **日期**: 2026-06-07  
> **关联分支**: `dev`  
> **关联文档**: `团队任务分配.md`（v1.4→v1.5）、`CLAUDE.md`、`CLI原生记忆设计方案.md`（v3.0→v3.1）  
> **关联提交**: `847e051`（CLI 记忆集成）、`5c4a2a3`（文档同步）

---

## 一、背景

项目在完成 C10 办公室聊天抽屉（commit `30f3c49`）后，MVP（P0）阶段已基本结束。用户引入了一份新的设计文档 `docs/CLI原生记忆设计方案.md`，希望评估是否可以融入现有项目，并同步规划下一阶段任务。

---

## 二、CLI 原生会话记忆集成

### 2.1 方案评估

对设计文档进行了全面审查，文档准确识别了当前架构 5 个核心痛点：
- 上下文膨胀（O(n) 增长）、系统提示词被淹没、格式不可控、重复计算、无法利用 CLI 原生记忆

**关键验证**：通过实机测试确认 `claude -p` + `--continue` 在 Windows 环境下完全可用，且按 cwd 自动隔离会话——不同 conversation workspace 的会话互不干扰。

发现并修正了文档中的事实错误：
| 原假设 | 实际 |
|--------|------|
| 会话存储 `{cwd}/.claude/sessions/` | `~/.claude/projects/<project>/<session-id>.jsonl` |
| 存在 `--cwd` 参数 | 不存在，cwd 通过子进程参数传递 |

### 2.2 阶段 1 实施（Claude Code only）

改动 4 个文件，净增约 30 行：

| 文件 | 改动 |
|------|------|
| `WebSocketController.java` | 删除 `buildContextString()` + `buildConversationContext()` 调用；context = `content.trim()` |
| `messages.py` | 新增 `_session_tracker: dict`；`is_first_message` 判断；`msg_end` 标记会话 |
| `claude_adapter.py` | 首轮注入 system_prompt + `-p`；后续 `--continue -p` |
| `main.py` | Windows `SelectorEventLoopPolicy` 修复 `NotImplementedError` |

三组端到端测试全部通过（首轮、多轮追问、会话隔离）。

### 2.3 文档同步

`CLI原生记忆设计方案.md` v3.0→v3.1，修正了所有存储路径引用，代码示例对齐实际实现，标注了阶段1完成/阶段2待定状态。

---

## 三、项目进度全面审计

### 3.1 各模块完成度

| 模块 | 本次更新前 | 本次更新后 | 变化说明 |
|------|:--:|:--:|------|
| 前端 (Vue 3) | ~92% | **~95%** | C10 办公室聊天抽屉完成 |
| 后端 Java | ~65% | **~72%** | CLI 记忆集成（context 简化） |
| Agent Service | ~80% | **~88%** | `_session_tracker` + `--continue` + Windows 修复 |

### 3.2 进度阶段判定

```
P0 (MVP):  ████████████████████ 100%  ← 可演示
P1:        ░░░░░░░░░░░░░░░░░░░░   0%  ← 待开始
P2:        ░░░░░░░░░░░░░░░░░░░░   0%  ← 仅有设计文档
```

**结论**: MVP 完整可演示，项目处于 P0→P1 过渡节点。

---

## 四、P1 阶段任务规划

### 4.1 任务总览

| # | 任务 | 负责人 | 涉及层 | 工作量 |
|---|------|--------|--------|:--:|
| P1-1 | 用户认证（Spring Security + JWT） | **B** + C | Java + 前端 | 大 |
| P1-2 | H2 → PostgreSQL 迁移 | **B** | Java | 小 |
| P1-3 | `_session_tracker` 持久化 | **D** | Python | 小 |
| P1-4 | Agent 元数据 Redis 共享缓存 | **A** + D | Java + Python | 中 |
| P1-5 | HttpApiAdapter（D9-D12） | **D** | Python | 中 |
| P1-6 | Agent 设置面板 | **C** | 前端 | 中 |
| P1-7 | 登录/注册页面 UI | **C** | 前端 | 中 |

### 4.2 成员分工

**成员 A（Java 后端）**：
- Redis 共享缓存集成 — Agent 元数据写入 Redis，Python 读取替代 config fallback
- 配合 B 完成 PostgreSQL 迁移测试

**成员 B（Java 基础设施）** — **P1 起点**：
- Spring Security + JWT 认证 — SecurityFilterChain、JWT Token 生成/验证、登录/注册接口
- H2 → PostgreSQL 迁移 — 改 5 行 YAML + SQL 语法调整

**成员 C（前端）**：
- 登录/注册页面 — LoginView + RegisterView + Pinia auth store + 路由守卫 + Axios Token 拦截器
- Agent 设置面板 — 会话级 Agent 配置浮层

**成员 D（Python Agent Service）**：
- HttpApiAdapter — 通用 HTTP API 适配器（Ollama/LocalAI/vLLM）
- `_session_tracker` 持久化 — 写入 `{workspace}/.tracker.json`，启动恢复
- Redis Agent 元数据读取 — 等 A 完成后对接

### 4.3 依赖关系

```
并行轨（低耦合，可同时开工）:
  轨1: B(PG迁移) → A(Redis) → D(Redis读取)
  轨2: B(JWT认证) → C(登录UI + Token适配)  ← 关键瓶颈
  轨3: D(HttpApiAdapter) [独立]
  轨4: D(tracker持久化) [独立]
  轨5: C(Agent设置面板) [独立]
```

3 条轨无依赖，D 和 C 可立即开始。轨 2 是唯一串行瓶颈（B→C）。

---

## 五、本次同步更新的文档

| 文档 | 更新内容 |
|------|---------|
| `CLAUDE.md` | 前端 95%/Java 72%/Agent 88%；WebSocketController/claude_adapter/messages 描述更新；新增 CLI native memory 条目；新增 Windows fix |
| `架构拓扑图与项目目录结构.md` | 拓扑流步骤 4-6 修正（context 简化 + session tracking）；目录树适配器描述；§1.3.1 标注"已实施" |
| `CLI原生记忆设计方案.md` | v3.0→v3.1；修正所有存储路径；代码对齐实际实现；标注阶段完成状态 |
| `团队任务分配.md` | v1.4→v1.5；进度数据修正；冲刺目标状态修正；新增完整 P1 阶段规划 |

---

## 六、经验总结

### 6.1 设计文档需要实机验证

设计文档提供了正确的架构方向，但存储路径假设（`{cwd}/.claude/sessions/` vs 实际的 `~/.claude/projects/`）和 `--continue` 可用性都需要在目标环境下验证。两条 `claude -p` 命令 5 分钟验证了核心假设，避免了基于错误假设的编码。

### 6.2 P0→P1 的决策边界

决定先将 Claude Code `--continue` 落地为 P0 收尾（而非推迟到 P1），因为：(1) 改动小（净增 30 行），(2) 立即获得 token 节省和上下文质量提升，(3) Codex 和 Orchestrator 等复杂任务留到 P1/P2 各有合适归属。

### 6.3 团队文档驱动开发

`团队任务分配.md` 作为任务追踪中枢，需要随项目进度频繁更新。本次将其从"任务清单"升级为"P0 完成状态 + P1 规划 + P2 预留"三层结构，使下一步工作方向一目了然。

### 6.4 依赖分析让并行开发成为可能

通过 5 条 P1 并行轨的清晰分析，3 条轨无前置依赖——成员 D（HttpApiAdapter、tracker 持久化）和成员 C（Agent 设置面板）无需等待其他人即可立即开工。
