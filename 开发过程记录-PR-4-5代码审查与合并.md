# 开发过程记录 — PR #4 & #5 后端功能代码审查、修复与合并

> **日期**: 2026-06-03\
> **关联分支**: `review/pr-4-artifact-service`、`review/pr-5-agent-controller` → `dev`\
> **所属模块**: backend-java\
> **原始 PR 概要**:
> - PR #4: 产物管理完整功能 — Artifact JPA Entity + Repository + Service（B4）+ REST 控制器（B5）
> - PR #5: Agent 管理 REST 控制器（B6）

---

## 一、背景

成员 B（@kyo19c）通过 fork 仓库 `kyo19c/Agenthub_demo` 向 `IAyousa/Agenthub_demo` 的 `dev` 分支提交了两个 PR：

| PR | 分支 | 标题 | 文件数 | 代码行 |
|----|------|------|--------|--------|
| #4 | `feat/artifact-service` | 产物管理（B4 + B5 合并）服务层 + REST 控制器 | 4 个新文件 | +362 行 |
| #5 | `feat/agent-controller` | Agent 管理（B6）REST 控制器 | 1 个新文件 | +131 行 |

两个 PR 都填补了后端 Controller 层的空白，是端到端联调的必要前提。作为成员 C（前端开发），需要对后端代码进行审查、修复问题、验证功能，然后合并到 dev。

### PR #4 变更内容

| 文件 | 说明 |
|------|------|
| `model/Artifact.java` | JPA Entity：UUID 主键 + `storedName`/`filename`/`fileSize`/`conversationId`/`messageId` + @PrePersist |
| `repository/ArtifactRepository.java` | `findByConversationId` / `findByConversationIdAndMessageId` |
| `service/ArtifactService.java` | 上传（UUID 防碰撞）、查询、下载、删除、@PostConstruct 孤儿文件恢复、路径穿越防护 |
| `controller/ArtifactController.java` | `POST /artifacts/upload` / `GET /artifacts/{id}` / `GET /conversations/{id}/artifacts` / `DELETE /artifacts/{id}` |

### PR #5 变更内容

| 文件 | 说明 |
|------|------|
| `controller/AgentController.java` | `GET /agents` / `POST /agents` / `GET /agents/{id}`，capabilities JSON 反序列化、isBuiltin 判定 |

---

## 二、审查过程

### 2.1 拉取远程 PR 分支到本地

由于 PR 来自 fork 仓库而非 origin，首先需要添加 fork 远程源并拉取分支：

```bash
# 添加 kyo19c fork 仓库
git remote add kyo19c https://gitee.com/kyo19c/Agenthub_demo.git

# 拉取两个 PR 分支
git fetch kyo19c feat/artifact-service feat/agent-controller

# 创建本地审查分支
git checkout -b review/pr-4-artifact-service kyo19c/feat/artifact-service
git checkout -b review/pr-5-agent-controller kyo19c/feat/agent-controller
```

### 2.2 PR #4 代码审查 — 产物管理

逐文件审查 `ArtifactService.java` 和 `ArtifactController.java`，发现 **3 个问题**：

| # | 问题 | 位置 | 严重程度 |
|---|------|------|----------|
| 1 | `init()` 孤儿文件恢复用文件名当主键 ID，与 `save()` 的 UUID 生成不一致 | `ArtifactService.java:57` | 🟡 中 |
| 2 | `delete()` 中文件删除 IOException 被静默吞掉（仅 `log.warn`），DB 记录照常删除 | `ArtifactService.java:159-163` | 🔴 高 |
| 3 | `download()` 错误响应的 `path` 字段使用磁盘 `storedName`，而非用户请求的 `id` | `ArtifactController.java:99` | 🟡 中 |

**额外改进项**：
- `save()` 中原始文件名未做路径穿越校验
- `validatePathSegment` 错误消息"路径参数"不够通用（现在也用于校验文件名）

### 2.3 PR #5 代码审查 — Agent 管理

审查 `AgentController.java`，发现 **3 个问题**：

| # | 问题 | 位置 | 严重程度 |
|---|------|------|----------|
| 1 | `POST /agents` 响应手动拼 Map 缺少 `avatarUrl` 和 `isBuiltin`，与 `GET /agents` 列表字段不一致 | `AgentController.java:71-78` | 🟡 中 |
| 2 | `POST /agents` 不支持 `avatarUrl` 入参，前端无法设置自定义头像 | `AgentController.java:39-42` | 💡 优化 |
| 3 | `isBuiltin` 判定依赖 `createdBy == null` 的隐式约定，无注释说明 | `AgentController.java:99` | 💡 优化 |

**额外改进项**：
- `type` 字段只检查非空，未验证是否为有效值（`claude_code`/`codex`/`custom`）
- `java.util.ArrayList` 导入但未使用

---

## 三、修复实施

### 3.1 PR #4 修复（`review/pr-4-artifact-service` 分支）

**修复 1 — init() 统一使用 UUID 作为 ID**：

```java
// 修复前：文件名当主键，与 save() 不一致
artifact.setId(storedName);
if (artifactRepository.existsById(storedName)) continue;

// 修复后：统一生成 UUID，按三元组去重
artifact.setId(java.util.UUID.randomUUID().toString());
boolean alreadyImported = artifactRepository
    .findByConversationIdAndMessageId(conversationId, messageId)
    .stream()
    .anyMatch(a -> storedName.equals(a.getStoredName()));
if (alreadyImported) continue;
```

**修复 2 — delete() 文件删除异常传播**：

```java
// 修复前：IOException 被静默吞掉
try { Files.deleteIfExists(filePath); }
catch (IOException e) { log.warn("Failed to delete..."); }
artifactRepository.delete(artifact);

// 修复后：checked 异常触发事务回滚 + 向上传播
@Transactional(rollbackFor = Exception.class)
public void delete(String id) throws IOException {
    try { Files.deleteIfExists(filePath); }
    catch (IOException e) {
        log.error("Failed to delete artifact file...");
        throw e;
    }
    artifactRepository.delete(artifact);
}
```

Controller 层同步增加 IOException 捕获：
```java
} catch (IOException e) {
    return error(HttpStatus.INTERNAL_SERVER_ERROR, "INTERNAL_ERROR",
            "产物文件删除失败，请查看服务端日志", "/artifacts/" + id);
}
```

**修复 3 — download() 错误路径使用请求 id**：

```java
// 修复前：serveFile 用 file.getName()（磁盘文件名）构造 path
private ResponseEntity<?> serveFile(java.io.File file) { ... }

// 修复后：传入 artifactId 用于错误响应 path
private ResponseEntity<?> serveFile(java.io.File file, String artifactId) { ... }
```

**额外改进**：
- `save()` 增加 `validatePathSegment(originalFilename)` 文件名路径穿越校验
- `validatePathSegment` 错误消息从"路径参数"改为"参数"（通用化）
- `download()` 增加 `log.warn` 记录 404 原因

### 3.2 PR #5 修复（`review/pr-5-agent-controller` 分支）

**修复 1 — POST /agents 响应复用 toSummary()**：

```java
// 修复前：手动拼 Map，缺 avatarUrl 和 isBuiltin
Map<String, Object> response = new LinkedHashMap<>();
response.put("id", saved.getId());
// ...

// 修复后：复用 toSummary()，自动包含 avatarUrl + isBuiltin
Map<String, Object> response = new LinkedHashMap<>(toSummary(saved));
response.put("systemPrompt", saved.getSystemPrompt());
response.put("createdAt", ...);
```

**修复 2 — 支持 avatarUrl 入参**：
```java
String avatarUrl = (String) body.get("avatarUrl");
agent.setAvatarUrl(avatarUrl != null ? avatarUrl.trim() : null);
```

**修复 3 — type 有效值校验**：
```java
private static final List<String> VALID_AGENT_TYPES = List.of("claude_code", "codex", "custom");

if (!VALID_AGENT_TYPES.contains(type.trim())) {
    return error(HttpStatus.BAD_REQUEST, "VALIDATION_ERROR",
            "type 必须是 claude_code、codex 或 custom", "/agents");
}
```

**修复 4 — isBuiltin 注释**：
```java
// data.sql 预置 Agent 未设 created_by（为 null），API 创建的设为 "system"
// 以此区分内置 Agent 与用户自定义 Agent
map.put("isBuiltin", agent.getCreatedBy() == null);
```

**额外改进**：移除未使用的 `import java.util.ArrayList`。

### 3.3 修复前后代码量对比

| PR | 修复前 | 修复后 | 变化 |
|----|--------|--------|------|
| #4 | ArtifactService 192 行 / ArtifactController 112 行 | 200 行 / 117 行 | +13 行 |
| #5 | AgentController 131 行 | 136 行 | +5 行 |

---

## 四、手动测试验证

> 测试采用**分步交互模式**：AI 提供测试步骤和预期结果，用户在另一个终端（Windows CMD / Git Bash）和 Postman 中执行命令，发现问题即时反馈，AI 调整方案后重测。

### 4.1 编译验证

```bash
cd backend-java && mvn compile -q
```

| # | 测试项 | 结果 |
|---|--------|:--:|
| 1 | PR #4 修改后编译 | ✅ 零错误 |
| 2 | PR #5 修改后编译 | ✅ 零错误 |

### 4.2 PR #4 — ArtifactController 端点测试

启动 Spring Boot（`mvn spring-boot:run`），在另一个终端执行 curl 命令。

**4.2.1 正常流程（4 个端点）**

| # | 端点 | 操作 | 预期 | 结果 |
|---|------|------|------|:--:|
| 1 | `POST /artifacts/upload` | 上传测试文件，指定 conversationId 和 messageId | 201 + 返回 id/filename/fileSize/conversationId/createdAt | ✅ |
| 2 | `GET /conversations/{id}/artifacts` | 按会话查询产物列表 | 200 + artifacts 数组包含上传的文件 | ✅ |
| 3 | `GET /artifacts/{id}` | 下载产物文件 | 200 + Content-Type 正确 + body 内容一致 | ✅ |
| 4 | `DELETE /artifacts/{id}` | 删除产物 | 204 No Content | ✅ |

**4.2.2 边界情况（6 项）**

| # | 测试场景 | 输入 | 预期 | 结果 |
|---|----------|------|------|:--:|
| 1 | 缺少 conversationId | 只上传 file，不传 conversationId | 400 | ✅ |
| 2 | conversationId 含 `..` | `conversationId=../etc` | 400 + "参数包含非法字符" | ✅ |
| 3 | 下载不存在的 ID | `GET /artifacts/non-existent-id` | 404 + "产物不存在" | ✅ |
| 4 | 删除不存在的 ID | `DELETE /artifacts/non-existent-id` | 400 + "产物不存在" | ✅ |
| 5 | 重复删除 | 删除已删除的产物 | 第一次 204，第二次 400 | ✅ |
| 6 | 删除后列表为空 | 删除后查询列表 | `{"artifacts": []}` | ✅ |

### 4.3 PR #5 — AgentController 端点测试

**4.3.1 正常流程（3 个端点）**

| # | 端点 | 操作 | 预期 | 结果 |
|---|------|------|------|:--:|
| 1 | `GET /agents` | 获取 Agent 列表 | 200 + 包含 data.sql 预置 Agent + isBuiltin=true | ✅ |
| 2 | `POST /agents` | 创建自定义 Agent（含 avatarUrl/capabilities） | 201 + 返回完整字段 + isBuiltin=false | ✅ |
| 3 | `GET /agents/{id}` | 获取单个 Agent 详情 | 200 + 含 systemPrompt + createdAt | ✅ |

**4.3.2 边界情况（3 项）**

| # | 测试场景 | 输入 | 预期 | 结果 |
|---|----------|------|------|:--:|
| 1 | 缺少 name | `{"type":"custom"}` | 400 + "name 不能为空" | ✅ |
| 2 | 非法 type | `{"name":"test","type":"invalid_type"}` | 400 + "type 必须是 claude_code、codex 或 custom" | ✅ |
| 3 | 不存在的 ID | `GET /agents/non-existent-id` | 404 + "Agent 不存在" | ✅ |

### 4.4 测试工具说明

用户根据测试场景灵活选择工具：

- **终端（Git Bash / CMD）**：编译验证（`mvn compile`）、服务启动（`mvn spring-boot:run`）、curl 命令测试 API
- **Postman**：可视化查看 JSON 响应、验证字段完整性，流式 SSE 测试更直观

Windows CMD 中 curl 不支持 `\` 多行续行符，单行命令可直接使用（注意 JSON 内部双引号的转义）：

```cmd
curl -X POST http://localhost:8080/agents -H "Content-Type: application/json" -d "{\"name\":\"测试Agent\",\"type\":\"custom\"}"
```

Git Bash 支持 `\` 换行，命令可读性更好：

```bash
curl -X POST http://localhost:8080/agents \
  -H "Content-Type: application/json" \
  -d '{"name":"测试Agent","type":"custom"}'
```

---

## 五、分支合并

### 5.1 合并策略

采用 **squash merge**，将 PR 原始提交 + 审查修复合并为一条干净的 commit，方便团队成员快速了解项目进度。

```bash
# 在审查分支上提交修复
git add <修复文件>
git commit -m "fix(backend): 审查修复..."

# 切换到 dev，squash 合并
git checkout dev
git merge --squash review/pr-4-artifact-service
git commit -m "feat(backend): 实现产物管理完整功能（PR #4）"
```

### 5.2 最终 dev 历史

```
466c204  feat(backend): 实现 Agent 管理 REST 控制器（PR #5）
9177bca  feat(backend): 实现产物管理完整功能 — 服务层 + REST 控制器（PR #4）
794edd6  feat(frontend): 补充ChatList搜索过滤与新建会话导航修复（C8增强+C5适配）
```

### 5.3 推送与 PR 关闭

```bash
git push origin dev
```

由于 squash merge 改变了 commit hash，Gitee 无法自动识别 PR 已合并。需要在 PR 页面贴上审查评论后**手动关闭**。

---

## 六、发现的问题与经验总结

### 6.1 PR #4

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | 孤儿文件 ID 格式不一致 | `init()` 用文件名当主键，`save()` 用 UUID | 统一为 `UUID.randomUUID()` + 三元组去重 |
| 2 | 文件删除失败静默吞掉 | `catch (IOException e) { log.warn }` 后继续删除 DB 记录 | `log.error + throw e` + `@Transactional(rollbackFor = Exception.class)` |
| 3 | 错误路径不直观 | `serveFile()` 用磁盘 `storedName` 而非用户请求的 `id` | 传参 `artifactId` 用于错误响应 |

### 6.2 PR #5

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | POST 响应字段不一致 | `create()` 手动拼 Map 而非复用 `toSummary()` | `new LinkedHashMap<>(toSummary(saved))` |
| 2 | type 无有效值校验 | 仅检查非空 | 新增 `VALID_AGENT_TYPES` 常量 + 校验 |
| 3 | isBuiltin 逻辑不透明 | `createdBy == null` 无注释 | 添加 2 行注释说明 data.sql 约定 |

### 6.3 Windows 环境下 curl 兼容性

用户在 CMD 中执行 curl 命令时遇到两个问题：

1. **不支持 `\` 续行符**：CMD 将 `\` 当作 URL 的一部分，导致 415 错误。解决：CMD 使用单行命令；Git Bash 支持 `\` 换行
2. **JSON 双引号转义**：CMD 需要 `\"` 转义，Git Bash 可用单引号包裹 JSON

**教训**：给 Windows 用户的测试命令应同时提供 CMD 和 Git Bash 两种格式。

### 6.4 Squash Merge 的取舍

用户最初对 commit message 中同时包含"原 PR 内容"和"审查修复细节"不满意，要求将修复细节移到 PR 评论区。最终策略：

- **commit message**：只描述功能（"做了什么"），保持历史简洁
- **Gitee PR 评论**：详细说明审查发现和修复细节（"怎么改的、为什么改"），供团队成员追溯

---

## 七、关键决策记录

| # | 决策 | 结论 | 理由 |
|---|------|------|------|
| 1 | 合并策略 | Squash merge（多个 commit → 一个 commit） | 每个 PR 在 dev 历史中只占一行，团队可直观了解进度 |
| 2 | 审查修复归属 | 修复细节写在 Gitee PR 评论，commit message 只写功能 | 保持 git 历史简洁，审查讨论留在 PR 平台 |
| 3 | 本地审查分支命名 | `review/pr-N-description` 格式 | 与 `feat/`/`fix/` 分支区分，一眼看出是审查用途 |
| 4 | 孤儿文件去重方式 | `findByConversationIdAndMessageId` + stream 过滤 `storedName` | 不新增 Repository 方法，利用现有查询 + 内存过滤（init 仅启动执行一次，性能无影响） |
| 5 | delete() 异常处理 | `@Transactional(rollbackFor = Exception.class)` + `throws IOException` | checked 异常也触发回滚，保证 DB 和磁盘状态一致 |

---

## 八、产物清单

| 文件 | 操作 | 行数变化 | 说明 |
|------|------|---------|------|
| `backend-java/.../model/Artifact.java` | 新增 | +43 | JPA Entity |
| `backend-java/.../repository/ArtifactRepository.java` | 新增 | +15 | Spring Data JPA |
| `backend-java/.../service/ArtifactService.java` | 新增 | +200 | 业务逻辑（含审查修复） |
| `backend-java/.../controller/ArtifactController.java` | 新增 | +117 | REST 控制器（含审查修复） |
| `backend-java/.../controller/AgentController.java` | 新增 | +136 | REST 控制器（含审查修复） |

### 代码统计

| 指标 | 数值 |
|------|------|
| 新增文件 | 5 |
| 修改文件 | 0 |
| 新增代码行 | 511 |
| 净减少代码行 | 0 |

---

## 九、后续任务依赖

| 任务 | 可开始时机 | 关联 |
|------|----------|------|
| C7 ChatWindow 重构 | 立即可开始 | 前端发送消息替换 mock 为真实 WebSocket |
| 前后端联调测试 | 成员 A 完成 ConversationController/MessageController（A5/A6）后 | 端到端：前端 → WebSocket → Java → Agent Service |
