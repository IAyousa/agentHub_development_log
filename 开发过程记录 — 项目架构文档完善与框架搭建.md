# 开发过程记录 — 项目整体架构文档完善与框架搭建

> **日期**: 2026-05-25\
> **任务编号**: D1\
> **所属模块**: docs / backend-java / agent-service\
> **前置依赖**: 无（纯工程搭建起点）

---

## 一、背景

### 1.1 为什么要做这个任务？

项目已具备五份核心设计文档（项目概述、数据模型、架构拓扑、API 契约、基础设施配置），但实际工程代码与文档之间存在多处偏差：

- Java 后端的 `application.yml` 使用了 H2 内存数据库，而文档约定使用 Docker 中的 PostgreSQL
- Python Agent 服务缺少 `config.py` 集中配置文件
- `main.py` 中引用了 `agents`、`conversations`、`messages`、`artifacts` 四个 endpoint 模块，但实际文件缺失
- 基础设施配置文档第 4.3 节（前端依赖）和第 5 节（.gitignore 配置）均为空白
- 文档缺少统一的快速启动检查清单，新成员上手困难

### 1.2 当前状态的限制

- Java 服务无法连接 PostgreSQL（本地未安装），但团队需要立即开始联调
- Python 启动后无法访问任何 API 端点（依赖的文件不存在）
- 文档信息不完整，无法作为"单一事实来源"指导开发

### 1.3 做完之后能解锁什么？

- 三端（前端 + Java + Python）服务均可独立启动并通过文档验证
- 文档与实际工程状态完全对齐，团队新成员可直接按文档操作
- 为后续任务（JPA 实体类创建、WebSocket 链路打通）提供可靠的运行环境

---

## 二、任务执行过程

### 2.1 Java 后端配置同步

**操作内容**：

检查 [pom.xml](file:///d:/HuaweiMoveData/Users/Yao/Desktop/Trae_project/backend-java/pom.xml) 和 [application.yml](file:///d:/HuaweiMoveData/Users/Yao/Desktop/Trae_project/backend-java/src/main/resources/application.yml) 与文档 [基础设施配置.md](file:///d:/HuaweiMoveData/Users/Yao/Desktop/Trae_project/docs/%E5%9F%BA%E7%A1%80%E8%AE%BE%E6%96%BD%E9%85%8D%E7%BD%AE.md) 的一致性。

发现以下偏差并修复：

| 偏差项 | 实际值 | 文档约定值 | 修复操作 |
|--------|--------|-----------|----------|
| 数据库 | H2 内存 | PostgreSQL | 保留 H2（开发便利性），同时补充 `application-dev.yml` 约定 |
| 服务名称 | `backend-java` | `agenthub` | 修改为 `agenthub` |
| 依赖项 | 缺少 validation | 需要 validation | 补充 `spring-boot-starter-validation` |
| Agent 服务地址 | 未配置 | `localhost:8000` | 添加 `agent.service.url` 配置 |
| 产物存储 | 未配置 | `./artifacts` | 添加 `artifact.storage` 配置 |

### 2.2 Python Agent 服务配置补全

**操作内容**：

1. 创建缺失的 endpoint 文件：
   - [messages.py](file:///d:/HuaweiMoveData/Users/Yao/Desktop/Trae_project/agent-service/app/api/endpoints/messages.py)：实现 `/chat/stream` 流式 mock 接口
   - [agents.py](file:///d:/HuaweiMoveData/Users/Yao/Desktop/Trae_project/agent-service/app/api/endpoints/agents.py)：返回 mock Agent 列表
   - [conversations.py](file:///d:/HuaweiMoveData/Users/Yao/Desktop/Trae_project/agent-service/app/api/endpoints/conversations.py)：返回空会话列表
   - [artifacts.py](file:///d:/HuaweiMoveData/Users/Yao/Desktop/Trae_project/agent-service/app/api/endpoints/artifacts.py)：返回空产物列表

2. 创建 [config.py](file:///d:/HuaweiMoveData/Users/Yao/Desktop/Trae_project/agent-service/config.py)，统一管理 Claude/Codex API 密钥、服务端口、CORS 白名单等配置。

3. 修改 [main.py](file:///d:/HuaweiMoveData/Users/Yao/Desktop/Trae_project/agent-service/main.py)，使用 `config.Settings` 替换硬编码的标题和 CORS 配置。

**设计决策**：

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 数据库选型 | 开发环境用 H2，文档约定用 PostgreSQL | H2 零配置启动，适合本地调试；PostgreSQL 是生产目标，文档保持一致性 |
| 配置文件结构 | 使用 `pydantic-settings` + `.env` | 符合 FastAPI 生态最佳实践，支持环境变量覆盖 |
| Mock 接口粒度 | 每个 endpoint 一个文件 | 与 main.py 的 router 注册一一对应，避免引入新路由时的认知负担 |

### 2.3 文档补全

**操作内容**：

逐项完善 [基础设施配置.md](file:///d:/HuaweiMoveData/Users/Yao/Desktop/Trae_project/docs/%E5%9F%BA%E7%A1%80%E8%AE%BE%E6%96%BD%E9%85%8D%E7%BD%AE.md)：

| 章节 | 补全前 | 补全后 |
|------|--------|--------|
| 4.3 前端依赖 | 空 | 列出核心框架、通信库、UI 库的实际版本号 |
| 5. .gitignore 配置 | 不存在 | 新增完整章节，分类列出依赖/环境/IDE/日志/Docker 的忽略规则 |
| 6. 快速启动检查清单 | 不存在 | 新增 7 步骤的表格，从数据库到前端页面逐步验证 |

### 2.4 三端服务启动与验证

1. **Python Agent 服务**（8000 端口）：端口被旧进程占用 → `taskkill` 清理 → 重启成功，`curl localhost:8000` 返回 `{"message":"Welcome to Agenthub API"}`
2. **Java 后端**（8080 端口）：Maven 本地仓库路径问题 → 使用 `-Dmaven.repo.local` 指定项目本地仓库 → 编译通过，H2 数据库初始化成功，WebSocket 端点 `/ws-chat` 激活
3. **Vue 前端**（5174 端口，因 5173 被占用自动切换）：`npm install` → `npm run dev` 启动成功

---

## 三、技术要点讲解

### 3.1 Maven 多模块项目的本地仓库隔离

当团队成员的 Maven 本地仓库（`~/.m2`）路径不一致或权限有问题时，可通过 `-Dmaven.repo.local=./.m2/repository` 将依赖下载到项目目录下，实现"自带依赖"的工程环境。缺点是需要额外占用磁盘空间（约 200MB+）。

### 3.2 H2 内存数据库 vs PostgreSQL

| 对比维度 | H2 内存模式 | PostgreSQL |
|----------|------------|-----------|
| 启动依赖 | 零配置 | 需 Docker 或本地安装 |
| 数据持久化 | 重启即丢失 | 持久保存 |
| SQL 方言 | 近似标准 SQL | 完整 PostgreSQL 方言 |
| 适用场景 | 开发初期快速迭代 | 集成测试、生产环境 |

本项目的策略：文档约定使用 PostgreSQL（作为"正确"配置参考），但 `application.yml` 默认配置为 H2，方便开发人员零门槛启动。

---

## 四、验证测试

### 4.1 自动化验证

| 测试项 | 命令 | 结果 |
|--------|------|------|
| Java 编译 | `mvn clean compile -f backend-java/pom.xml` | BUILD SUCCESS |
| Python 服务响应 | `curl http://localhost:8000/` | `{"message":"Welcome to Agenthub API"}` |
| Java 服务启动 | `mvn spring-boot:run` | `Started AgenthubApplication in 5.6s` |
| H2 控制台可访问 | 浏览器访问 `/h2-console` | 登录页正常显示 |

### 4.2 手动功能测试清单

| # | 测试项 | 预期行为 | 结果 |
|---|--------|---------|------|
| 1 | Python Agent 服务启动 | `uvicorn` 输出 `Uvicorn running on http://0.0.0.0:8000` | 通过 |
| 2 | Java 后端 WebSocket 端点 | 启动日志包含 `SimpleBrokerMessageHandler` Started | 通过 |
| 3 | 前端页面加载 | 浏览器访问 `localhost:5174` 看到聊天界面 | 通过 |
| 4 | 文档与代码版本一致 | `main.py` 导入 `config.py` 无报错 | 通过 |
| 5 | `.gitignore` 覆盖全栈 | `git status` 不显示 `node_modules`、`target`、`__pycache__` | 通过 |

---

## 五、关键决策记录

| # | 决策 | 结论 | 理由 |
|---|------|------|------|
| 1 | 开发阶段数据库选型 | H2 内存模式 | 零配置启动，团队成员无需安装 Docker/PostgreSQL |
| 2 | 生产数据库约定 | PostgreSQL | 支持 JSONB 类型、完整方言、运维成熟 |
| 3 | 文档中的 `application.yml` 示例 | 展示 PostgreSQL 配置 | 作为"目标态"参考，避免新成员误以为 H2 是生产方案 |
| 4 | Python 配置文件结构 | `pydantic-settings` + `.env` | 类型安全、支持环境变量覆盖、FastAPI 生态标准 |
| 5 | 文档目录结构 | 五份独立 Markdown 文件 | 按关注点分离，便于按需查阅和独立更新 |
| 6 | Git 提交策略 | 结构化的分步 Conventional Commits | 便于 Code Review 和回溯变更历史 |
| 7 | 日志忽略范围 | 覆盖 npm/yarn/pnpm + JVM hs_err | 消除不同包管理器和 Java 崩溃日志的污染风险 |

---

## 六、产物清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `backend-java/pom.xml` | 修改 | 新增 H2、validation 依赖 |
| `backend-java/src/main/resources/application.yml` | 修改 | 切换为 H2 配置，补齐 Agent/Artifact 配置 |
| `agent-service/config.py` | 新增 | 集中管理 Claude/Codex 密钥和服务配置 |
| `agent-service/.env` | 新增 | 本地环境变量模板 |
| `agent-service/requirements.txt` | 修改 | 补充 `pydantic-settings`、`httpx`、`sse-starlette` 等 |
| `agent-service/main.py` | 修改 | 使用 `config.Settings` 替代硬编码 |
| `agent-service/app/api/endpoints/messages.py` | 新增 | 流式聊天 mock 接口 |
| `agent-service/app/api/endpoints/agents.py` | 新增 | Agent 列表 mock 接口 |
| `agent-service/app/api/endpoints/conversations.py` | 新增 | 会话列表 mock 接口 |
| `agent-service/app/api/endpoints/artifacts.py` | 新增 | 产物列表 mock 接口 |
| `docs/基础设施配置.md` | 修改 | 补全前端依赖、.gitignore、快速启动清单三节 |
| `.gitignore` | 修改 | 整合团队统一规则，增强日志忽略覆盖 |

---

## 七、数据流/架构影响

本次任务未改变核心数据流，但建立了统一的环境契约：

```
开发人员入职
  → 阅读 docs/基础设施配置.md
  → 执行第 6 章"快速启动检查清单"（7 步）
  → 三端服务启动成功
  → 继续阅读 API 契约文档 + 数据模型文档
  → 开始编码
```

---

## 八、后续任务依赖

| 任务 | 可开始时机 | 关联 |
|------|----------|------|
| JPA 实体类创建（User, Conversation, Message, Agent） | 本次完成后立即 | 对照 `docs/数据模型设计.md` 创建 Java 实体 |
| WebSocket 端到端链路打通 | JPA 实体类完成后 | 前端 → Java → Python 全链路消息流式传输 |
| Claude/Codex 适配器开发 | Python 服务稳定运行后 | 在 `agent-service/adapters/` 下完成真实 AI 调用 |
| 前端聊天界面与 STOMP 集成 | Java WebSocket 端点就绪后 | `chat.ts` store 集成真实 WebSocket 连接 |
