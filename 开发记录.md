# AgentHub Java 后端开发记录

**项目**: AgentHub 多Agent协作平台 - Java后端服务
**开发周期**: 2026-5-23 ~ 2026-06-10
**技术栈**: Spring Boot 3.2.5 + Spring Data JPA + Spring Data Redis + H2 / PostgreSQL + STOMP WebSocket
**核心职责**: Agent 注册管理、会话生命周期管理、消息持久化、Agent网关调用、WebSocket 实时推送、Redis 共享缓存、数据库迁移

---

## 一、核心业务模块 (A1–A7)

### A1. ConversationService — 会话全生命周期管理
- **文件**: `service/ConversationService.java`
- **实现**: 完整覆盖 `create / findById / findAll / update / delete / updateAgents` 六个核心方法
- **关联逻辑**: 支持多 Agent 与会话的关联，空 `agentIds` 自动回退到默认 `claude_code`
- **提交来源**: commit `fb9848e`

### A2. MessageService — 消息持久化与上下文构建
- **文件**: `service/MessageService.java`
- **实现要点**:
  - 消息写入与合法性校验（必填字段非空、会话归属校验）
  - 分页查询 `findByConversationIdOrderByCreatedAtDesc`
  - 上下文构建器 `buildConversationContext`，供 Agent 调用时拼装历史
  - 消息 `pin / unpin` 置顶与取消置顶
- **提交来源**: commit `fb9848e`

### A3. AgentGatewayService — Agent 调用网关（完全重写）
- **文件**: `service/AgentGatewayService.java`
- **核心方法 `sendToAgent()`**:
  1. 构造请求体（camelCase 字段对齐 Python API 契约）
  2. 调用 FastAPI `POST /api/agent/chat` SSE 流式端点
  3. 按 `data: <JSON>\n\n` 标准 SSE 格式逐行解析
  4. 内部类 `AgentToken` 提供三种构造器：标准 token、`finish()`、`error()`，便于统一分发
- **关键特性**: 与 Python 端字段契约完全一致（camelCase），无额外字段转换层

### A4. WebSocketSessionManager — STOMP 会话管理
- **文件**: `service/WebSocketSessionManager.java`
- **技术选型**: `SimpMessagingTemplate` + `ConcurrentHashMap`
- **API**: `register / unregister / sendToConversation / sendToUser`
- **提交来源**: PR #7（commit `79a1046`）

### A5. ConversationController — 会话 REST API
- **文件**: `controller/ConversationController.java`
- **覆盖端点**: 7 个（对照 API 文档 2.2 节）
  - `GET /api/conversations` 列表
  - `GET /api/conversations/{id}` 详情
  - `POST /api/conversations` 创建
  - `PUT /api/conversations/{id}` 更新
  - `DELETE /api/conversations/{id}` 删除
  - `PATCH /api/conversations/{id}/agents` 关联Agent变更
  - `POST /api/conversations/{id}/messages` 发送消息（WebSocket 升级入口）
- **关键约束**: `agentIds` 为可选字段，空数组时默认走 `claude_code`
- **提交来源**: PR #7（commit `79a1046`）

### A6. MessageController — 消息 REST API
- **文件**: `controller/MessageController.java`
- **覆盖端点**:
  - `GET /api/conversations/{id}/messages?page=&size=` 分页消息列表
  - `PATCH /api/messages/{id}/pin` 置顶 / 取消置顶（含会话归属校验，防止越权）
- **对照文档**: API 文档 2.2.7–2.2.8 节
- **提交来源**: PR #7（commit `79a1046`）

### A7. WebSocketController — 消息流处理（完善版）
- **文件**: `controller/WebSocketController.java`
- **处理流水线**:
  1. 保存当前用户消息（持久化）
  2. Agent 路由决策（根据 `agentIds` 选择目标 Agent）
  3. **仅传递当前消息**（由 Python CLI 通过 `--continue` 自行管理会话上下文，避免重复拼装）
  4. 调用 `AgentGatewayService.sendToAgent()` 获取 SSE 流
  5. 通过 `WebSocketSessionManager.sendToConversation()` 推送 STOMP 消息
- **架构调整**: 移除原 `buildContextString()` 历史拼装逻辑，上下文管理下沉到 Python CLI
- **提交来源**: PR #7 + PR #6 + CLI 记忆集成

---

## 二、P1 子任务：架构增强

### P1-A1. Redis 共享缓存集成
**目标**: Java 后端写入 Agent 元数据到 Redis，Python 服务直接读取，彻底替代 `config.py` 中的 `AGENT_REGISTRY` fallback，支持热更新。

**新增文件 1**: `config/RedisConfig.java`
- 使用 `LettuceConnectionFactory` 作为连接工厂
- 配置 `RedisTemplate<String, Object>`
- 序列化策略：key 使用 `StringRedisSerializer`，value 使用 `RedisSerializer.json()`（Spring Data Redis 3.2 标准 API，支持类型信息）
- 支持对象级存储，兼容 Python 端 JSON 读取

**新增文件 2**: `service/AgentCacheService.java`
- `saveAgent(Agent)` — 写入单个 Agent
- `saveAgentsBatch(List<Agent>)` — 批量写入 + 更新 `agent:list`
- `getAgentById(String id)` — 按 ID 查询
- `getAllAgents()` — 获取全量列表

**缓存 Key 规范**:
- `agent:metadata:{id}` — 单 Agent 元数据
- `agent:list` — 全量 Agent 列表

**配置变更**:
- `pom.xml` 新增 `spring-boot-starter-data-redis` 依赖（含 Lettuce）
- `application.yml` 新增 Redis 连接配置（`localhost:6379`，Lettuce 连接池参数）

**热更新机制**: Java 更新 Agent → Redis 写入即时生效 → Python 服务下次读取自动获得最新配置，无需重启。

### P1-A2. PostgreSQL 迁移配合
**目标**: 配合成员 B 完成 H2 → PostgreSQL 生产数据库迁移。

**配置文件**: `application.yml` + JPA 自动建表配置
- H2 为开发环境默认（`file:./shared-data/agenthub`，AUTO_SERVER 模式）
- PostgreSQL 为生产环境：`postgresql://{host}:5432/agenthub`
- JPA 策略：`hibernate.ddl-auto=update`，迁移时先切换至 `validate` 校验 schema 一致
- 驱动切换：H2 `org.h2.Driver` ↔ PG `org.postgresql.Driver`

**迁移验证清单**:
- [x] `AgentRepository` — CRUD + 自定义查询
- [x] `ConversationRepository` — 分页 + 用户过滤
- [x] `MessageRepository` — 按会话分页、置顶过滤
- [x] `UserRepository` — 用户基础 CRUD
- [x] 所有外键约束在 PG 上正确建立
- [x] `LocalDateTime` 字段映射为 PG `TIMESTAMP`
- [x] UUID 主键映射为 PG `UUID` 类型

---

## 三、技术要点与架构决策

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 连接管理 | STOMP over WebSocket + `ConcurrentHashMap` | 原生 Spring 支持，分布式部署可平滑替换为 Redis Pub/Sub |
| Agent 调用 | SSE 流解析 + 内部 `AgentToken` 统一模型 | 与 Python FastAPI 契约零摩擦，流式响应即时推送 |
| 上下文管理 | Python CLI `--continue` 自管理 | 避免 Java 重复拼装历史，减少跨语言序列化不一致风险 |
| 缓存策略 | Redis 作为 Agent 注册表单一数据源 | Python 不再依赖本地配置，支持热更新、跨实例一致 |
| 数据库 | H2(开发) / PostgreSQL(生产) | 开发环境零依赖启动，生产环境高性能 PG |
| 序列化 | `RedisSerializer.json()` | Spring 官方推荐，自动处理多态类型，Java ↔ Python 互读 |

---

## 四、文件清单汇总

```
src/main/java/com/agenthub/
├── config/
│   ├── RedisConfig.java              [P1-A1]  ✅ 新增
│   ├── CorsConfig.java
│   ├── SecurityConfig.java
│   └── WebSocketConfig.java
├── controller/
│   ├── ConversationController.java   [A5]     ✅ 7 端点
│   ├── MessageController.java        [A6]     ✅ 分页 + 置顶
│   └── WebSocketController.java      [A7]     ✅ 流处理流水线
├── service/
│   ├── ConversationService.java      [A1]     ✅ 生命周期管理
│   ├── MessageService.java           [A2]     ✅ 持久化 + 上下文
│   ├── AgentGatewayService.java      [A3]     ✅ SSE 调用网关
│   ├── AgentCacheService.java        [P1-A1]  ✅ Redis 缓存服务
│   └── WebSocketSessionManager.java  [A4]     ✅ STOMP 会话管理
├── model/                            (Agent / Conversation / Message / User)
├── repository/                       (4 个 JPA Repository)
└── AgenthubApplication.java

src/main/resources/
└── application.yml                   [P1-A1 + P1-A2] ✅ Redis + PG 配置
```

