# AgentHub 技术框架文档 — 架构评审问题清单
>版本: v1.0\
>创建日期: 2026-06-03\
>用途: 团队成员后续开发参考 + AI 编程工具上下文注入\
>来源: 基于 v1.2 技术框架文档的架构评审

## 问题总览
| 编号 | 问题 | 严重程度 | 涉及层次 | 状态 |
| --- | --- | --- | --- | --- |
| P1 | 上下文构建格式已标准化 | ✅ 已修复 | Java ↔ Python | API 契约 §4.2 已定义 messages[] 格式；MessageService.buildContext() 已实现 |
| P2 | 消息置顶 API 路由设计存在安全隐患 | ✅ 已修复 | REST API | API 契约 §2.2.8 已更新为 `/conversations/{conversationId}/messages/{messageId}/pin` |
| P3 | agent_switch 事件契约已增强 | ✅ 已修复 | WebSocket 协议 | API 契约 §3.3.2 每个 MessageChunk 均携带 agentId；MessageChunk.java 已含该字段 |
| P4 | 产物上传流程与架构不匹配 | ✅ 已修复 | REST API / 内部 HTTP | API 契约 §2.4.4 已新增 `/internal/artifacts` 端点定义 |
| P5 | 错误推送格式已标准化 | ✅ 已修复 | WebSocket / 全局 | API 契约 §5.3-5.5 已新增错误码枚举 + 各层级异常捕获职责 |
---
## P1：上下文构建格式未定义
### 问题描述
Java 端的 MessageService.buildContext() 需要从数据库取出历史消息，组装成 context 字符串，通过 HTTP 传给 Python Agent 服务。但在当前内部 HTTP 接口定义中，context 字段是裸字符串，其格式没有任何规范约束。

### 影响范围
Java 端可能格式化为 "用户：xxx\nAgent：xxx"，Python 端可能期望 [{"role":"user","content":"xxx"}] 结构。

格式不一致会导致 Agent 无法正确理解对话历史，表现为“忘记上文”或“回复混乱”。

两端各自按自己理解实现，联调时必然出现集成故障。

### 当前状态
内部 HTTP 接口文档（API 契约定义与通信协议规范 第 4.2 节）中，AgentChatRequest 的 context 字段定义为：
```json
{
  "context": "帮我写一个 React 计数器组件"
}
```
#### 缺失内容：
没有定义 Java 端如何从 messages 表中读取历史并格式化

没有定义 Python 端如何解析 context 并注入到 Agent 调用

没有示例说明多轮对话的上下文格式
### 修复方案
#### 方案 A（推荐）：结构化的 `messages` 数组
理由：与 Claude API / OpenAI API 的 messages 格式天然兼容，Python 端无需二次解析。
##### Java 端构建逻辑：
```java
// MessageService.java
public List<Map<String, Object>> buildContext(String conversationId) {
    List<Message> historyMessages = messageRepository
        .findTop20ByConversationIdOrderByCreatedAtAsc(conversationId);

    List<Map<String, Object>> messages = new ArrayList<>();
    for (Message msg : historyMessages) {
        Map<String, Object> entry = new LinkedHashMap<>();
        if ("user".equals(msg.getSenderType())) {
            entry.put("role", "user");
            entry.put("content", extractText(msg.getContent()));
        } else {
            entry.put("role", "agent");
            entry.put("agentName", msg.getAgentName());
            entry.put("content", extractText(msg.getContent()));
        }
        messages.add(entry);
    }
    return messages;
}
```
##### Python 端解析逻辑：
```python
# main.py
@app.post("/api/agent/chat")
async def agent_chat(request: ChatRequest):
    # request.messages 已经是结构化数组，直接使用
    context_text = format_context(request.messages)
    # ...
```
##### 更新后的 `AgentChatRequest` 模型：
```python
# models.py
from pydantic import BaseModel
from typing import List, Optional

class ContextMessage(BaseModel):
    role: str            # "user" | "agent"
    content: str
    agentName: Optional[str] = None

class ChatRequest(BaseModel):
    agentType: str = "claude_code"
    systemPrompt: str = ""
    messages: List[ContextMessage] = []   # ← 替代原来的 context: str
    stream: bool = False
    workingDirectory: Optional[str] = None
```
#### 方案 B：约定纯文本格式
```text
# 格式规范（需要两端严格遵守）
用户：帮我写一个 React 组件
Agent（Claude Code）：好的，这是代码...
用户：修改一下颜色
```
缺点：Python 端需要编写解析逻辑（正则/字符串分割），容易受特殊字符干扰，不推荐。
### 涉及文件
| 文件 | 改动 |
| --- | --- |
| agent-service/models.py | ChatRequest 增加 messages: List[ContextMessage] 字段 |
| backend-java/.../service/MessageService.java | 实现 buildContext() 方法，返回 List<Map> |
| backend-java/.../dto/AgentChatRequest.java | context 字段改为 messages 数组 |
| backend-java/.../service/AgentGatewayService.java | 请求体构造适配新字段 |
---
## P2：消息置顶 API 路由设计存在安全隐患
### 问题描述
当前消息置顶接口设计为：
```http
PUT /messages/{id}/pin
```
#### 问题分析：
缺少会话归属校验：{id} 是 UUID，无法从路径推断它属于哪个会话。后端如果不做权限校验，用户 A 可以置顶用户 B 的会话中的消息。

REST 资源层级断裂：消息是会话的子资源，URL 应该体现这种层级关系。/messages/{id} 跳过了父资源，不符合 RESTful 设计规范。

前端调用链路冗余：前端需要先拿到 messageId（通常从消息列表中提取），再单独调用此接口。如果改为层级路径，前端可以直接用已有的 conversationId + messageId 调用，无需额外状态管理。
#### 修复方案
##### 推荐路径：
```http
PUT /conversations/{conversationId}/messages/{messageId}/pin
```
##### 请求体：
```json
{
  "pinned": true
}
```
##### 后端校验逻辑：
```java
// ConversationController.java
@PutMapping("/conversations/{conversationId}/messages/{messageId}/pin")
public ResponseEntity<?> pinMessage(
    @PathVariable String conversationId,
    @PathVariable String messageId,
    @RequestBody PinRequest request) {

    // 1. 查询消息，校验 message 是否属于该 conversation
    Message message = messageRepository.findById(messageId)
        .orElseThrow(() -> new NotFoundException("消息不存在"));

    if (!message.getConversationId().equals(conversationId)) {
        throw new BadRequestException("消息不属于此会话");
    }

    // 2. 更新置顶状态
    message.setIsPinned(request.getPinned());
    messageRepository.save(message);

    return ResponseEntity.ok(Map.of("id", messageId, "pinned", request.getPinned()));
}
```
##### 前端适配：
```javascript
// 调用方式更自然
await axios.put(
  `/conversations/${conversationId}/messages/${messageId}/pin`,
  { pinned: true }
);
```
#### 涉及文件
| 文件 | 改动 |
| --- | --- |
| backend-java/.../controller/ConversationController.java | 新增 PUT /conversations/{conversationId}/messages/{messageId}/pin 端点 |
| frontend/src/api/conversation.js | API 调用路径更新 |
| 技术文档 API 契约部分 | 路径从 /messages/{id}/pin 更新为层级路径 |
---
## P3：`agent_switch` 事件契约不完整
### 问题描述
当前 WebSocket 协议定义了 agent_switch 事件：
```json
{
  "type": "agent_switch",
  "agentId": "agent_codex_001",
  "agentName": "Codex"
}
```
#### 问题分析：
作用域不明确：如果用户同时打开多个会话窗口（多 Tab 或会话列表），前端无法区分 agent_switch 事件属于哪个会话。因为 WebSocket 订阅地址 /topic/conversation.{id} 已经限定了范围，所以此问题影响较小，但多 Tab 场景仍需注意。

事件丢失风险：由于流式 chunk 和 agent_switch 事件是独立推送的，在高并发或网络抖动时，可能出现“先收到 chunk，后收到 agent_switch”的情况，导致第一个 chunk 用错误的头像渲染。

职责单一性不足：agent_switch 事件独立于消息流，增加了前端的状态管理复杂度——需要维护“当前正在发言的 Agent”这一额外状态。

### 修复方案
推荐：每个 MessageChunk 都携带 agentId（冗余但健壮）

##### 更新后的 MessageChunk 格式：
```json
{
  "content": "这是代码片段",
  "isComplete": false,
  "agentId": "agent_claude_001",
  "agentName": "Claude Code",
  "messageType": "text",
  "messageId": null
}
```
##### 前端渲染逻辑简化：
```javascript
// chatStore.js - 收到 chunk 时的处理
function handleIncomingMessage(chunk) {
  // 始终以当前 chunk 中的 agentId 为准
  streamingMessage.value = {
    agentId: chunk.agentId,        // 每个 chunk 都有，不存在丢失问题
    agentName: chunk.agentName,
    content: streamingMessage.value.content + chunk.content,
    isStreaming: !chunk.isComplete
  };
}
```
与 agent_switch 事件的关系：

P1 阶段保留：agent_switch 可作为独立的控制事件发送，用于触发“正在切换 Agent”的过渡动画

P1 阶段增强：agent_switch 事件中增加 conversationId 字段，明确作用域

核心保障：每个 MessageChunk 都携带 agentId，即使 agent_switch 丢失也不影响正确渲染
### 涉及文件
| 文件 | 改动 |
| --- | --- |
| backend-java/.../dto/MessageChunk.java | 增加 agentId 字段 |
| backend-java/.../controller/WebSocketController.java | 推送 chunk 时填充 agentId |
| agent-service/main.py | SSE chunk 中携带 agentId |
| frontend/src/stores/chatStore.js | 渲染逻辑简化，以 chunk.agentId 为准 |
| 技术文档 WebSocket 协议部分 | 更新 MessageChunk 字段说明 |
---
## P4：产物上传流程与架构不匹配
### 问题描述
当前 REST API 定义了：
```http
POST /artifacts/upload
Content-Type: multipart/form-data
```
#### 问题分析：
在当前架构中，产物的生产者是 Python Agent 服务（或 Agent 服务驱动的 CLI 工具），而不是前端用户。

逻辑流程：
```text
Agent 生成代码 → Python Agent 服务接收 → ??? → 前端上传 → Java 后端存储
```
### 修复方案
##### 新增内部产物接口（服务器间调用）：
```http
POST /internal/artifacts
```
##### 请求体（JSON 格式，无需 multipart）：
```json
{
  "conversationId": "conv_abc123",
  "messageId": "msg_002",
  "filename": "App.jsx",
  "content": "import React from 'react';\n\nconst App = () => {\n  return <div>Hello</div>;\n};",
  "contentType": "text/javascript"
}
```
##### Java 端实现逻辑：
```java
// ArtifactController.java
@PostMapping("/internal/artifacts")
public ResponseEntity<ArtifactDTO> createArtifact(
    @RequestBody CreateArtifactRequest request) {

    // 1. 保存内容到本地文件系统（或 MinIO）
    String filePath = artifactService.saveContent(
        request.getFilename(),
        request.getContent()
    );

    // 2. 数据库记录
    Artifact artifact = new Artifact();
    artifact.setConversationId(request.getConversationId());
    artifact.setMessageId(request.getMessageId());
    artifact.setFilename(request.getFilename());
    artifact.setFilePath(filePath);
    artifact.setContentType(request.getContentType());
    artifact = artifactRepository.save(artifact);

    // 3. 返回制品信息（前端用 URL 直接预览）
    return ResponseEntity.status(201).body(ArtifactDTO.from(artifact));
}
```
##### 调用链路（更新后）：
```text
Agent 生成代码
  → Python Agent 服务接收
  → HTTP POST /internal/artifacts → Java 后端
  → Java 后端存储文件 + 数据库记录
  → WebSocket 推送 preview_card 消息给前端（含 URL）
  → 前端直接 iframe 加载 URL 预览
```
前端用户的 POST /artifacts/upload 接口保留：供未来“用户手动上传文件作为上下文”的场景使用（P2 功能）。

### 涉及文件
| 文件 | 改动 |
| --- | --- |
| backend-java/.../controller/ArtifactController.java | 新增 POST /internal/artifacts 端点 |
| backend-java/.../dto/CreateArtifactRequest.java | 新增请求 DTO |
| agent-service/adapters/claude_adapter.py | 解析 CLI 输出中的文件路径，调用内部接口上传 |
| 技术文档 API 契约部分 | 新增内部接口定义 |
---
## P5：错误推送格式未标准化
### 问题描述
当前 WebSocket 错误处理（API 契约定义与通信协议规范 第 5.3 节）定义了错误消息格式：
```json
{
  "content": "抱歉，Agent 服务暂时不可用，请稍后重试。",
  "isComplete": true,
  "agentName": "System",
  "messageType": "error",
  "messageId": "msg_error_001"
}
```
#### 问题分析：
标准化已存在但未在所有层级贯彻：文档定义了格式，但没有明确哪些异常应该在哪个层级被捕获并转换为这个格式。

缺少错误码枚举：前端需要根据不同的错误类型做不同的 UI 处理（重试按钮 vs 仅提示 vs 引导用户刷新），仅有 messageType: "error" 不够。

### 修复方案
#### 定义标准错误码枚举：
| 错误码 | 说明 | 前端行为 |
| --- | --- | --- |
| AGENT_TIMEOUT | Agent 调用超时 | 显示“重新发送”按钮 |
| AGENT_UNAVAILABLE | Agent 服务不可用 | 显示“稍后重试”提示 |
| CLI_NOT_FOUND | 本地 CLI 工具未安装 | 显示配置引导 |
| CONTEXT_TOO_LONG | 上下文超出 token 限制 | 提示用户固定关键消息或开启新会话 |
| UNKNOWN_ERROR | 未分类的错误 | 显示通用错误提示 |
#### 更新后的错误推送格式：
```json
{
  "isComplete": true,
  "agentName": "System",
  "messageType": "error",
  "errorCode": "AGENT_TIMEOUT",
  "content": "Agent 响应超时，请稍后重试。",
  "messageId": "msg_error_001",
  "retryable": true
}
```
#### 各层级异常捕获职责：
| 层级 | 捕获范围 | 转换后的 errorCode |
| --- | --- | --- |
| Python Agent 服务 | 本地 CLI 调用失败（subprocess.CalledProcessError） | CLI_NOT_FOUND |
| Python Agent 服务 | 自定义 Agent HTTP 调用超时（httpx.TimeoutException） | AGENT_TIMEOUT |
| Java AgentGatewayService | Python Agent 服务连接失败（ConnectException） | AGENT_UNAVAILABLE |
| Java AgentGatewayService | WebClient 超时 | AGENT_TIMEOUT |
| Java WebSocketController | 其他未捕获异常 | UNKNOWN_ERROR |
#### 前端处理示例：
```javascript
// chatStore.js
function handleIncomingMessage(chunk) {
  if (chunk.messageType === 'error') {
    messages.value.push({
      senderType: 'system',
      errorCode: chunk.errorCode,
      content: chunk.content,
      retryable: chunk.retryable,
      messageId: chunk.messageId
    });
  }
}
```
```vue
<!-- MessageBubble.vue 错误消息渲染 -->
<template>
  <div v-if="message.messageType === 'error'" class="error-bubble">
    <p>{{ message.content }}</p>
    <button v-if="message.retryable" @click="handleRetry">重新发送</button>
  </div>
</template>
```
### 涉及文件
| 文件 | 改动 |
| --- | --- |
| backend-java/.../dto/MessageChunk.java | 增加 errorCode 和 retryable 字段 |
| backend-java/.../controller/WebSocketController.java | 异常处理中设置正确的 errorCode |
| backend-java/.../service/AgentGatewayService.java | onErrorResume 中标准化错误格式 |
| agent-service/main.py | 异常处理器转换为标准错误格式 |
| frontend/src/components/chat/MessageBubble.vue | 错误消息气泡样式 + 重试按钮 |
| 技术文档错误处理部分 | 增加错误码枚举和各层级职责定义 |
---
## 修复优先级建议
| 阶段 | 修复项 | 理由 |
| --- | --- | --- |
| 当前立即修复 | P1（上下文格式） | 阻塞 Agent 联调，没有它多轮对话无法运行 |
| P1 阶段修复 | P4（产物上传） | 当前流程绕路，实现后能直接展示 Agent 生成的代码预览 |
| P1 阶段修复 | P5（错误格式） | 提升用户体验，减少“系统故障”的困惑感 |
| P2 阶段优化 | P2（置顶路由） | 当前功能可用，优化 API 设计 |
| P2 阶段增强 | P3（agent_switch） | 多 Agent 群聊场景下才真正需要 |
