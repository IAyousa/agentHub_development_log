# 开发过程记录 — PR#14 审查合并与项目成果卡片重构

> **日期**: 2026-06-10
> **任务编号**: PR#14-review
> **所属模块**: agent-service / backend-java / frontend
> **前置依赖**: PR#14 zsy125（Agent工作空间隔离+项目打包下载）✅

---

## 一、背景

### 1.1 需求来源

团队成员 zsy125 提交了 PR#14，实现了：
- Agent 工作空间隔离（每会话独立目录，注入隔离规则到 system prompt）
- 项目打包 ZIP 下载功能

该 PR 已合并到 `review/pr-14-workspace-bundle` 分支，需要经过 Code Review、Bug 修复、重构优化后合入 `dev`。

### 1.2 审查前状态

PR#14 功能可运行，但存在以下问题：
1. **产物卡片分散**：`artifact_uploader`（stdout 代码块检测）和 `workspace_scanner`（磁盘文件扫描）各自独立推送 WebSocket 卡片，用户看到多张碎片化卡片
2. **iframe 无法渲染完整网页**：Agent 生成的 HTML/CSS/JS 多文件项目，HTML 中的相对引用无法解析
3. **刷新后卡片丢失**：`project_bundle` 仅通过 WebSocket 实时推送，未持久化，刷新浏览器后降级为离散预览卡片
4. **多个运行时 Bug**：had_error 未初始化、数据结构不匹配、401/404 等

### 1.3 审查目标

- 统一产物卡片为单张可折叠文件树（`project_bundle`）
- 修复 8 个已发现的 Bug
- 确保刷新前后卡片展示一致
- 按团队规范分批评审、分批提交到 `dev`

---

## 二、任务执行过程

### 阶段一：问题诊断与根因定位

#### 2.1 复现环境

启动三层服务（Vue 前端 :5173 + Spring Boot :8080 + FastAPI :8000），发送测试用例：

> 请使用工具在工作目录中创建 index.html、style.css 和 script.js 三个文件，内容是一个简单的计数器网页

#### 2.2 诊断发现的 3 个核心问题

**问题 1：workspace_scanner 扫描到 0 个文件**

Python 日志：
```
[workspace_scanner] Snapshot: 0 prev files, 0 current files
[workspace_scanner] Changed files: 0 -> []
[artifact_uploader] Batch uploaded 3/3 files → project_bundle
```

**根因**：`artifact_uploader` 从 Agent stdout 文本中用正则提取 Markdown 代码块（不需要磁盘文件），而 `workspace_scanner` 扫描实际磁盘。Agent 只在响应中输出了代码块文本，并未通过 Write 工具将文件实际写入工作目录。

两个来源的文件名也不同：

| 来源 | 文件名 | 数据来源 |
|------|--------|---------|
| artifact_uploader | `artifact_001.html`, `artifact_002.css`, `artifact_003.js` | stdout 代码块 |
| workspace_scanner | `index.html`, `style.css`, `script.js` | 磁盘文件 |

**问题 2：iframe 无法渲染完整网页**

`ProjectBundleCard.vue` 注入 `<base>` 标签来解析 HTML 中的相对引用（`style.css`、`script.js`），Java 端有 `/artifacts/conversation/{id}/{filename}` 端点按文件名查找 artifact。

但文件名不匹配：HTML 引用 `style.css`，而 artifact 存储为 `artifact_002.css` → 404。

另外 `<base>` 注入使用 `html.replace(/<head[^>]*>/i, ...)` — HTML5 允许省略 `<head>` 标签，导致注入失败。

**问题 3：刷新浏览器后卡片降级**

`project_bundle` 仅在 `ArtifactController.java:192` 作为 WebSocket `messageType` 推送，从未调用 `messageService.saveMessage()` 存入数据库。刷新后前端从 REST API 加载 artifact，每个映射为独立的 `artifact_preview` 卡片。

---

### 阶段二：架构重构 — 统一 project_bundle 卡片

#### 2.3 artifact_uploader 文件名推断

**文件**：`agent-service/app/utils/artifact_uploader.py`

新增 `_infer_filename()` 函数，从代码块前 300 字符中匹配文件名模式：

```python
_FILENAME_HINT_RE = re.compile(
    r"(?:\*\*|`|###\s+|(?:(?:创建|新建|写入|文件|File|文件路径|保存为)[:：]?\s*))"
    r"([a-zA-Z0-9_\-./]+\.(?:html?|css|jsx?|tsx?|py|java|json|xml|ya?ml|md|sql|sh|go|rs|txt))",
    re.IGNORECASE,
)

def _infer_filename(text_before_block: str, ext: str, index: int) -> str:
    for match in _FILENAME_HINT_RE.finditer(text_before_block):
        filename = match.group(1).strip()
        if Path(filename).suffix.lower() == ext:
            return filename
    return f"artifact_{index + 1:03d}{ext}"
```

效果：`artifact_001.html` → `index.html`，`artifact_002.css` → `style.css`

#### 2.4 代码块自动写盘

新增 `write_blocks_to_workspace()`：

```python
async def write_blocks_to_workspace(blocks: list[dict], workspace_path: str) -> list[str]:
    written = []
    for block in blocks:
        filename = block.get("filename", "")
        filepath = os.path.join(workspace_path, filename)
        if os.path.exists(filepath):
            continue  # 保护 Agent 实际写入的文件
        os.makedirs(os.path.dirname(filepath) or workspace_path, exist_ok=True)
        with open(filepath, "w", encoding="utf-8") as f:
            f.write(block["content"])
        written.append(relpath)
    return written
```

设计意图：stdout 中检测到的代码块写入磁盘 → 下次 `workspace_scanner` 能扫到 → 后续消息可实现增量快照。

#### 2.5 _upload_merged_artifacts 串行化重构

**文件**：`agent-service/app/api/endpoints/messages.py`

重构前（并行，结果被丢弃）：
```python
code_files, scan_files = await detect_and_upload(...), await scan_and_upload(...)
all_files = [f for f in (code_files or []) if f]
# ... 合并后 all_files 未使用
```

重构后（串行，scanner 优先）：
```
1. detect_code_blocks(full_text)       → 代码块检测（不立即上传）
2. write_blocks_to_workspace(blocks)   → 写盘
3. scan_and_upload(conv_id, msg_id, wd) → scanner 扫描磁盘→上传（权威来源）
4. 若 scanner 未找到文件 → 回退到 artifact_uploader 直接上传代码块
```

**关键设计决策**：scanner 是磁盘文件的权威来源 — 它扫到文件就直接结束，不再用 `artifact_uploader` 补传。避免文件名推断失败导致的重复上传。

#### 2.6 前端 project_bundle 卡片

**新文件**：`frontend/src/components/chat/ProjectBundleCard.vue`

功能：
- 折叠文件树：点击展开/收起，显示文件名、语言标签、文件大小
- HTML 文件：iframe `srcdoc` 预览，注入 `<base>` 标签解决相对路径引用
- 非 HTML 文件：代码高亮预览
- 下载全部按钮：ZIP 打包下载
- `<base>` 注入兼容无 `<head>` / 无 `<html>` 的 HTML5 页面：

```typescript
if (/<head[^>]*>/i.test(html)) {
    injected = html.replace(/<head[^>]*>/i, m => m + baseTag)
} else if (/<html[^>]*>/i.test(html)) {
    injected = html.replace(/<html[^>]*>/i, m => m + '<head>' + baseTag + '</head>')
} else {
    injected = '<head>' + baseTag + '</head>' + html
}
```

#### 2.7 刷新持久化 — artifact 按 messageId 聚合

**文件**：`frontend/src/stores/chat.ts` — `loadMessages()`

```typescript
// 按 messageId 分组
const byMessageId = new Map<string, typeof artifacts>()
for (const a of artifacts) {
    const key = a.messageId || `_lonely_${a.id}`
    if (!byMessageId.has(key)) byMessageId.set(key, [])
    byMessageId.get(key)!.push(a)
}
// 同组 ≥2 → project_bundle；孤立 → artifact_preview
for (const [msgId, group] of byMessageId) {
    if (group.length >= 2 && msgId && !msgId.startsWith('_lonely_')) {
        messages.push({ type: 'project_bundle', metadata: { files: ... } })
    } else {
        // 保持原有 artifact_preview 行为
    }
}
```

---

### 阶段三：Bug 修复

#### 2.8 修复清单

| # | 提交 | 问题 | 根因 | 修复 |
|---|------|------|------|------|
| 1 | `9a5fe8d` | `_ext` NameError 导致 script.js 丢失 | `_ext()` 定义在 workspace_scanner.py 中，artifact_uploader.py 中未定义 | 在 artifact_uploader.py 中添加 `import os` + `from pathlib import Path` + `_ext()` |
| 2 | `9a5fe8d` | 文件名推断失败触发重复上传 | `_infer_filename` 猜错时回退到 `artifact_NNN.ext`，与 scanner 文件名不匹配 → extra_blocks 逻辑误判 → 二次上传 | 删除文件名差集比对逻辑，scanner 扫到文件直接结束 |
| 3 | `9a5fe8d` | 刷新后 project_bundle 变为离散预览卡片 | `project_bundle` 未持久化到 Message 表 | loadMessages 按 messageId 聚合 artifact |
| 4 | `9a5fe8d` | iframe 无法渲染省略 `<head>` 的 HTML | `<base>` 注入正则只匹配 `<head>` 标签 | 三级回退：head → html → 直接拼接 |
| 5 | `1378f60` | orchestrator had_error 未初始化 | 变量在使用前可能未赋值 | 声明时初始化为 False |
| 6 | `67792cb` | iframe HTML 资源 404 | 按 artifactId 查找，未支持按文件名查找 | 新增 `findByConversationIdAndFilename` 端点 |
| 7 | `09221cd` | 项目成果卡片预览 401 | SecurityConfig 未放行 `/artifacts/**` | `permitAll()` 放行 |
| 8 | `206af0b` | Java Map vs Python List 不匹配 | API 契约不一致 | Python 侧适配 `files` 字段提取 |

#### 2.9 慢启动误触发 fallback 修复（测试发现）

**问题**：Python 重启后第一条消息，有时先出现 "当前 Agent 服务尚未连接 CLI 工具" 的 fallback，紧接着又出现正常回复。

**根因链路**：
```
Python 冷启动 → Orchestrator._plan() Claude CLI 首次调用慢
→ plan 解析失败 → steps = [] → fallback 单Agent
→ Claude 冷启动产出零文本 → adapter 的 msg_end fallback 触发
→ Java 收到 finish=true 作为第一个 SSE 事件
→ receivedTokens=false → 立即发送 fallback 消息
```

**修复（两层防线）**：

1. **adapter 层**（`claude_adapter.py`）：增加 `has_content` 守卫
```python
has_content = False  # 跟踪是否产生过 msg_chunk
# 在 text_delta 处理中：
has_content = True
# 结尾补发 msg_end 时：
if not has_error and not msg_ended:
    if not has_content:
        yield {"type": "msg_chunk", "delta": "（Agent 已完成处理，未产生文本输出）"}
    yield {"type": "msg_end", ...}
```

2. **Java 层**（`WebSocketController.java`）：延迟 2s 确认 fallback
```java
if (token.isFinish()) {
    if (receivedTokens[0]) {
        // 正常路径
        messageService.saveMessage(...)
        pushFinish(topic, agentType, token);
    } else {
        // 延迟 2s — 给慢启动的 Agent 时间
        CompletableFuture.delayedExecutor(2, TimeUnit.SECONDS).execute(() -> {
            if (receivedTokens[0]) {
                // 延迟期间 token 到达了 → 正常保存
                messageService.saveMessage(...)
            } else {
                // 确实没有 token → 发送 fallback
                messagingTemplate.convertAndSend(topic, fallback);
            }
            pushFinish(topic, agentType, token);
        });
    }
}
```

---

## 三、技术要点讲解

### 3.1 双源检测 → 单卡片：为什么 scanner 是权威来源

```
artifact_uploader：stdout → 正则提取代码块 → 文件名推断（可能猜错）
workspace_scanner：磁盘 → os.walk → SHA256 快照 → 增量 diff → 真实文件名
```

scanner 拿到的文件名是真实的磁盘路径（`index.html`），而 artifact_uploader 依赖正则推断（可能回退到 `artifact_001.html`）。所以架构上 scanner 优先：
- scanner 扫到文件 → 直接上传 + 推送卡片 → artifact_uploader 跳过
- scanner 扫不到 → 说明 Agent 没写盘 → artifact_uploader 作为回退上传 stdout 代码块

### 3.2 WebFlux SSE 异步回调中的数组状态共享

```java
boolean[] receivedTokens = {false};
CompletableFuture.delayedExecutor(2, SECONDS).execute(() -> {
    if (receivedTokens[0]) { /* token 延迟到达 */ }
});
```

Java lambda 捕获的是数组引用，不是数组的副本。异步任务和 SSE 回调共享同一个 `receivedTokens` 引用，这是延迟确认机制能工作的关键。

### 3.3 asyncio.ensure_future 的异常静默

```python
asyncio.ensure_future(_upload_merged_artifacts(...))
```

`ensure_future` 创建的是 fire-and-forget 任务。如果协程内部抛异常且未被捕获，异常只在任务被 GC 时以 `Task exception was never retrieved` 告警形式出现，不会传播到主流程。这解释了为什么 `_ext` NameError 没有导致 HTTP 500，而是静默丢失了 script.js。

---

## 四、验证测试

### 4.1 自动化验证

| 测试项 | 命令 | 结果 |
|--------|------|------|
| Python 语法检查 | `python -c "import ast; ast.parse(...)"` | ✅ artifact_uploader.py / messages.py / claude_adapter.py 通过 |
| TypeScript 类型检查 | `npx vue-tsc --noEmit` | ✅ 零错误 |
| Java 编译 | `mvn compile -q` | ✅ 通过 |

### 4.2 手动功能测试清单

| # | 测试项 | 预期行为 | 结果 |
|---|--------|---------|------|
| 1 | Agent 创建 3 个文件（HTML+CSS+JS） | 项目成果卡片显示 `index.html`/`style.css`/`script.js`（非 artifact_NNN） | ✅ |
| 2 | 展开 index.html iframe 预览 | 完整计数器页面（数字+按钮+样式） | ✅ |
| 3 | workspace_scanner 能扫到文件 | Python 日志：`N current files, M changed files`（N≥3） | ✅ |
| 4 | 刷新浏览器后卡片保持 | 仍为 project_bundle 折叠文件树，非离散预览卡片 | ✅ |
| 5 | 无重复卡片（旧 Bug） | 不会出现文件名不同的重复 project_bundle | ✅ |
| 6 | Python 重启后第一条消息 | 不出现 "CLI 尚未连接" fallback | ✅ |
| 7 | 单文件产出 | 仍正常显示为 artifact_preview 预览卡片 | ✅ |
| 8 | CSS 静态资源在 iframe 正确加载 | Network 无 404 | ✅ |

---

## 五、关键决策记录

| # | 决策 | 结论 | 理由 |
|---|------|------|------|
| 1 | 双源检测的优先顺序 | workspace_scanner 优先，artifact_uploader 回退 | scanner 有真实文件名，artifact_uploader 文件名靠正则推断可能猜错 |
| 2 | artifact_uploader 文件名策略 | 推断真实文件名，失败回退 artifact_NNN.ext | 优先匹配 iframe 相对引用；回退确保不丢文件 |
| 3 | 代码块写盘策略 | 写盘但同名文件跳过 | 保护 Agent Write 工具创建的真实文件不被覆盖 |
| 4 | fallback 修复方案 | 两层防线（adapter has_content + Java 2s 延迟） | adapter 层确保不产空 msg_end；Java 层覆盖所有竞态路径 |
| 5 | 刷新后 artifact 聚合策略 | 按 messageId 分组，≥2 个合并为 project_bundle | 与 WebSocket 推送的 project_bundle 逻辑一致 |
| 6 | 合并策略 | Squash merge 后拆为 3 个语义化 commit | 遵循团队规范（Conventional Commits），便于团队成员追踪功能链路 |
| 7 | `<base>` 标签注入策略 | 三级回退（head → html → 裸拼接） | 兼容 HTML5 省略 `<head>` 的写法 |

---

## 六、产物清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `agent-service/app/utils/workspace_scanner.py` | 新增 | 工作区文件快照+增量diff+批量上传 |
| `agent-service/app/utils/artifact_uploader.py` | 修改 | 文件名推断 `_infer_filename()` + 写盘 `write_blocks_to_workspace()` + `import os, Path` |
| `agent-service/app/api/endpoints/messages.py` | 修改 | `_upload_merged_artifacts` 串行化重构：检测→写盘→扫描→回退 |
| `agent-service/adapters/claude_adapter.py` | 修改 | 增加 `has_content` 守卫，防止空 msg_end |
| `agent-service/orchestrator.py` | 修改 | `had_error` 初始化为 False |
| `agent-service/config.py` | 修改 | AGENT_WORKSPACE_ROOT 可配置，CLAUDE_STREAM_ARGS |
| `agent-service/prompts/system_prompts.py` | 修改 | 注入工作区隔离指令 |
| `backend-java/.../ArtifactController.java` | 修改 | `/internal/artifacts/batch` 批量端点 + `/artifacts/conversation/{id}/{filename}` 按名查找 + `/conversations/{id}/download` ZIP下载 |
| `backend-java/.../WebSocketController.java` | 修改 | workspaceRoot 可配置 + finish 延迟 2s 确认 fallback + pushFinish 方法抽取 |
| `backend-java/.../SecurityConfig.java` | 修改 | 放行 `/artifacts/**` 和 `/conversations/*/download` |
| `backend-java/.../WebSocketConfig.java` | 修改 | 原生WS + SockJS双模式端点 |
| `backend-java/.../ArtifactRepository.java` | 修改 | `findByConversationIdAndFilename` |
| `backend-java/.../ArtifactService.java` | 修改 | `findByConversationIdAndFilename` |
| `frontend/src/components/chat/ProjectBundleCard.vue` | 新增 | 可折叠文件树卡片，iframe 预览，base 标签注入，下载全部 |
| `frontend/src/components/chat/ChatMessage.vue` | 修改 | 新增 project_bundle 类型渲染 |
| `frontend/src/stores/chat.ts` | 修改 | project_bundle 类型 + loadMessages 按 messageId 聚合 artifact + 流式 token 队列 |
| `frontend/src/websocket/wsClient.ts` | 修改 | 原生 WebSocket 支持 |
| `docs/Agent项目生成与交付规划.md` | 新增 | 配套设计文档 |

### 代码统计

| 指标 | 数值 |
|------|------|
| 新增文件 | 3 |
| 修改文件 | 16 |
| 新增代码行 | +1534 |
| 净减少代码行 | -218 |

---

## 七、数据流/架构影响

### 7.1 产物管线（变更后）

```
Agent stdout → Python 流式解析
  ├─ text_delta → SSE → Java → STOMP → 前端 Markdown 渲染（逐 token）
  └─ msg_end 触发:
       ├─ detect_code_blocks() → 推断文件名 → write_blocks_to_workspace()
       ├─ workspace_scanner.scan_and_upload() → 磁盘扫描 → POST /internal/artifacts/batch
       │   └─ Java 存文件 + WS push project_bundle（1.5s 延迟）
       └─ 若 scanner 未找到文件 → detect_and_upload() 回退

前端 project_bundle 卡片:
  ├─ 实时: WS handleToken → project_bundle → ProjectBundleCard
  └─ 刷新: loadMessages → 按 messageId 聚合 artifact → project_bundle
       └─ 展开 HTML: fetch previewUrl → 注入 <base> → iframe srcdoc
```

### 7.2 慢启动 fallback 改动

```
旧: finish 先到 → 立即发 "CLI 尚未连接" → finish chunk → 正常 reply 作为第二条消息
新: finish 先到 → 等 2s → 若 token 延迟到达 → 正常保存+finish
                        → 若确实无 token → 发 fallback+finish
```

---

## 八、后续任务依赖

| 任务 | 可开始时机 | 关联 |
|------|----------|------|
| http_adapter.py（D13） | 现在可开始 | 使用相同的 adapter 基类和 config 模式 |
| Agent settings panel（前端） | 现在可开始 | 与 project_bundle 卡片无直接耦合 |
| CI/CD 集成测试 | dev 分支稳定后 | 需要完整的端到端测试覆盖产物管线 |
| P2 Redis 共享缓存 | 需要 Redis 环境 | 与当前改动无冲突 |

---

## 附：关键错误与修复记录

| # | 错误现象 | 根因 | 修复 |
|---|---------|------|------|
| 1 | `NameError: name '_ext' is not defined` → script.js 丢失 | `_ext()` 只定义在 workspace_scanner.py | artifact_uploader.py 添加 `import os, Path` + `_ext()` |
| 2 | project_bundle 重复出现 2 张卡片 | `_infer_filename` 猜错文件名触发 extra_blocks 二次上传 | 删除文件名差集比对，scanner 优先 |
| 3 | 刷新后 project_bundle 变 artifact_preview | project_bundle 未持久化 | loadMessages 按 messageId 聚合 |
| 4 | "CLI 尚未连接" 假报错 | 冷启动时 msg_end 先于 msg_chunk 到达 Java | adapter has_content + Java 2s 延迟 |
| 5 | 重复卡片显示 6 个文件（3 真+3 重名） | 旧 bug 已在 DB 留下重复 artifact | 需用新会话测试，旧会话数据需手动清理 |
