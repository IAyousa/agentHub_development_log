# 开发过程记录 — 团队任务分配与群聊方案设计

> **日期**: 2026-05-26\
> **参与方式**: 用户（项目负责人） + AI Agent（Claude Code）协作\  
> **产出**: `团队任务分配.md`（完整更新版）

---

## 1. 背景

在完成 JPA 数据层搭建、种子数据预置、data.sql 中文编码修复之后，项目需要将开发工作分配给 4 名团队成员。同时，前端已有的 OfficeView（SVG 办公室场景）需要确定其在整体架构中的定位——是作为独立的"办公室"模块，还是作为"群聊"的一种 UI 形态。

本次交互持续约 2 小时，经历了**需求分析 → 接口探索 → 方案对比 → 逐步收敛 → 最终决策**的完整过程。

---

## 2. 交互过程

### 阶段一：团队任务分配初版

**用户请求**：参照项目进度，为团队成员分配任务。

**AI 执行**：
1. 读取 `CLAUDE.md` 了解项目整体状态（前端 ~60%、后端 ~25%、Agent ~25%）
2. 扫描三个模块的实际文件目录，确认已完成和未完成的内容
3. 按模块制定 4 名成员的任务分配方案

**关键发现**：
- 前端 ChatView 的 `simulateResponse()` 是纯本地 mock，无任何真实后端调用
- 后端 WebSocketController 的 handler 是空壳（`// TODO`）
- 所有 Service 层代码均未实现

**产出**：第一版 `团队任务分配.md`，包含 8 个章节（进度、分工、依赖、冲刺、WS 格式、REST API、分支规范、环境速查）。

---

### 阶段二：CORS 策略讨论

**用户提问**：CORS 是否需要在代码中写死地址？用 Nginx 会不会更麻烦？

**AI 分析**：
- 对比了 CorsConfig 方案（写死/读 yml 配置）和 Nginx 反向代理方案
- 结论：**分阶段使用**。开发阶段用 CorsConfig（从 yml 读取，不硬编码），部署阶段用 Nginx

**关键认知**：让 4 人团队各自在本地装 Nginx 增加环境搭建成本，不如写一个 CorsConfig 类。部署时 Nginx 接管后，CorsConfig 可以直接删除。

---

### 阶段三：基于前端源码的接口需求分析

**用户请求**：读取前端项目内容，列出可能需要哪些接口。

**AI 执行**：
1. 逐文件阅读前端所有源码：`stores/chat.ts`、`App.vue`、`ChatWindow.vue`、`ChatList.vue`、`ChatMessage.vue`、`OfficeView.vue`、`CreateOfficeModal.vue`、`SideBar.vue`
2. 从每个组件的数据来源推断需要的后端接口

**关键发现**：

| 前端位置 | 当前数据来源 | 缺失的接口 |
|----------|-------------|-----------|
| ChatList 的 `mockChats` | 硬编码 3 个 Agent | Agent 列表 + 会话列表 |
| ChatWindow 的 `simulateResponse()` | 本地 setTimeout 模拟 | WebSocket 收发 |
| chatStore 的 `conversations` | 硬编码 2 个会话 | 会话/消息 CRUD |
| OfficeView 的 `offices` | 硬编码 1 个办公室 | 办公室/成员管理 |

**产出**：完整的 20 个 REST 接口 + 2 个 WebSocket topic 清单，并发现 Office 和 Artifact 在后端无对应 JPA Entity。

---

### 阶段四：办公室模型设计 — Office = Group Conversation

**用户提问**：办公室相当于群聊，应该怎么融入现有框架？

**AI 分析**：
1. 审查现有 `Conversation.java` 的 `type` 字段（`direct` / `group`）——说明设计之初已预留群聊
2. 审查 `conv_agents` 多对多关联表——会话可绑定多个 Agent

**设计方案**：
- **不新建 Office 实体**，复用 `Conversation(type=group)`
- 新增 `ConversationMember` 实体（用户 ↔ 会话多对多）
- 扩展 `Conversation` 增加 `description`、`theme`、`max_members` 字段
- `seatIndex` 不放后端，纯前端 UI 逻辑

```
单聊 direct: 1 user + N agents（通过 conv_agents）
群聊 group:  M users + N agents（通过 conv_agents + conversation_members）
```

---

### 阶段五：模式澄清 — 用户质疑复杂度

**用户提问**：单聊是 1 用户 vs 1 Agent 还是 vs 多 Agent？多聊（多用户+多 Agent）在当前阶段是否太复杂？

**AI 反思与回应**：

根据 `conv_agents` 多对多表的设计，原意是：
- **单聊 = 1 个用户 + N 个 Agent**（一个会话里可以同时和多个 Agent 对话）
- **群聊 = M 个用户 + N 个 Agent**（多个真人 + 多个 AI）

AI 认同用户的直觉：**群聊在当前阶段确实太复杂**。原因：
- 需要用户认证体系（登录/JWT）
- 需要成员权限管理
- 需要在线状态同步
- 需要消息广播逻辑

**决策**：P0 阶段只做单聊（`type=direct`），群聊推迟到 P2。`type` 字段和 `conv_agents` 表已为群聊预留扩展空间，不阻塞当前开发。

---

### 阶段六：ChatList 概念纠偏

**用户提问**：前端中间栏是展示 Agent 和用户的会话列表吗？

**AI 通过源码验证**：当前 ChatList 的 `mockChats` 展示的是 **Agent 列表**（Orchestrator、Coder、Designer），而非会话列表。这与后端模型（Conversation 可绑定多个 Agent）不一致。

**问题本质**：前端 mock 数据把 Agent 当作 Conversation，1 Agent = 1 Conversation；但后端模型是 N Agent ↔ 1 Conversation。

**建议**：ChatList 改为展示**用户的会话列表**，每个会话可包含多个 Agent。这是成员 C 的 C8 任务。

---

### 阶段七：方案 A vs 方案 B 对比

**用户提出方案**：中间栏展示会话列表，办公室页面变为多 Agent 群聊。单 Agent 用 ChatView，多 Agent 用 OfficeView——两种会话记录都展示在中间栏。

**对比分析**：

| 维度 | 方案 A（两种 UI） | 方案 B（统一 ChatView） |
|------|------------------|------------------------|
| 现有代码利用率 | 高 — OfficeView SVG 保留 | 低 — SVG 被浪费 |
| 用户体验差异化 | 强 — 有独立"空间感" | 弱 — 多 Agent 场景不明显 |
| 代码重复风险 | 有 — 需抽取共享组件 | 无 |
| 概念清晰度 | 高 — 单聊=小窗、群聊=办公室 | 一般 |

**决策**：选择**方案 A**，但必须做一层抽象——ChatMessage 和 MessageInput 抽成共享组件，ChatView 和 OfficeView 只是两套"皮肤"，共用同一套消息引擎。**消息逻辑只写一遍**。

---

### 阶段八：Agent 指派机制设计

**用户提问**：如何实现指派一个 Agent 完成任务，或一个 Agent 指派其他 Agent 共同完成？

**AI 分析**：

现有架构中 **Orchestrator（调度器）** 就是为此设计的。它是 Python Agent Service 层的核心：

```
用户发消息
  → Orchestrator 用 LLM 分析意图
    → 拆解为子任务
      → 子任务 1：派给 Coder（代码生成）
      → 子任务 2：派给 Designer（UI 设计）
    → 汇总结果
  → 流式返回（每 token 携带 agentId，前端据此切换头像和名称）
```

**关键设计点**：
- 职责完全在 Python Agent Service 层，前端不感知
- 用户无需手动选择 Agent，Orchestrator 自动判断
- WebSocket 推送的 `agentId` 字段让前端能区分"这句话是谁说的"
- 已有的 `conv_agents` 表 + `Message.sender_id` 字段完全支持

---

### 阶段九：OfficeView 聊天功能确认

**用户确认**：目前的办公室 UI 没有聊天功能。

**AI 通过源码确认**：`App.vue` 中 Office 视图只有 SideBar + OfficeView，OfficeView 的内容是纯 SVG 场景（桌子、椅子、人物动画、办公室管理弹窗），**没有任何消息列表或输入框**。

**补充设计**：采用**方式 2（底部抽屉）**——聊天面板从底部滑出，保留 SVG 场景的沉浸感。

```
┌──────┬──────────────────────────────────┐
│ 导航  │      SVG 办公室场景              │
│      ├──────────────────────────────────┤
│      │  聊天面板（可折叠/展开）           │
│      │  嵌入共享 ChatMessage+MessageInput │
└──────┴──────────────────────────────────┘
```

---

### 阶段十：汇总并更新团队任务文档

**用户请求**：汇总所有讨论结论，更新团队成员任务文档。

**AI 执行**：对 `团队任务分配.md` 进行了全面修订：

1. **新增第 2 章「核心设计决策」**：7 项关键决策的完整说明
2. **更新成员 A**：明确 A1 只做 direct 单聊，核心链路注释增加 agentId
3. **更新成员 B**：B1 CorsConfig 从 yml 读取；B6/B7 DTO 增加 agents 列表和 agentId 字段
4. **更新成员 C**（最大变动）：9→11 项任务
   - 新增 C6：提取 MessageInput 共享组件
   - 新增 C10：OfficeView 底部聊天面板
   - C7：ChatWindow 重构为组装共享组件
   - C8：ChatList 从 Agent 列表改为会话列表
   - C9：路由增加 type 判断
5. **更新成员 D**：D4 Orchestrator 改为核心调度器；D5 增加 Orchestrator 自身 Prompt；D6 SSE 响应携带 agentId/agentName
6. **更新冲刺目标**：P0 增加 ChatList 真实会话；P2 增加 OfficeView 底部面板 + 群聊模式
7. **更新 WebSocket 格式**：增加 `agentName` 字段和 Agent 切换通知

---

## 3. 关键决策记录

| # | 决策 | 结论 | 影响范围 |
|---|------|------|----------|
| 1 | CORS 策略 | 开发用 CorsConfig（yml 读地址），部署用 Nginx | 成员 B 的 B1 |
| 2 | 单聊定义 | 1 用户 + N Agent（非 1v1） | 成员 A/D 的消息模型 |
| 3 | 群聊优先级 | P2，当前不做 | 整体计划推迟 2-3 周 |
| 4 | 中间栏内容 | 会话列表，非 Agent 列表 | 成员 C 的 C8 |
| 5 | 群聊 UI 方案 | 方案 A：两种 UI + 共享消息引擎 | 成员 C 的 C6/C7/C10 |
| 6 | Office 聊天布局 | 方式 2：底部抽屉 | 成员 C 的 C10 |
| 7 | Agent 指派 | Orchestrator 自动调度，前端不感知 | 成员 D 的 D4/D6 |
| 8 | 数据模型 | 群聊=P2，当前不建 conversation_members | 无阻当前开发 |
| 9 | 接口范围 | P0 仅 7 个接口，砍掉 13 个群聊/办公室/认证接口 | 所有成员任务范围 |

---

## 4. 对团队开发的启示

### 4.1 AI Agent 辅助开发的典型模式

本次交互展示了 AI Agent 在软件开发中的几个有效使用模式：

**模式一：源码驱动的需求分析**
- AI 通过逐文件阅读前端源码，从 mock 数据中逆向推断需要的后端接口
- 比凭记忆或凭经验估算更准确，避免了遗漏

**模式二：方案对比决策**
- 每次遇到设计选择（CORS、UI 方案、Office 模型），AI 列出表格对比优劣
- 用户基于直观的对比做决策，而非被 AI 单方面推荐

**模式三：逐步收敛**
- 从 20+ 接口 → 7 个 P0 接口，从群聊复杂方案 → 只做单聊
- 每次迭代都在缩小范围、降低复杂度

### 4.2 有用的人工判断点

以下环节用户的判断起到了关键作用：

- **质疑群聊复杂度**：用户凭直觉判断"多用户+多 Agent 在当前阶段太复杂"，AI 认同并收敛范围
- **方案 A 选择**：用户主动提出的中间栏展示会话 + OfficeView 多 Agent 方案，既保留了已完成的 SVG 动画投资，又保持了概念清晰
- **Office 聊天布局选择**：用户选择方式 2（底部抽屉），兼顾沉浸感和可用性

### 4.3 产出成果

| 成果 | 文件 | 用途 |
|------|------|------|
| 完整任务分配 | `团队任务分配.md` | 4 名成员各 7-11 项任务，含文件路径、说明、依赖 |
| 7 项设计决策 | `团队任务分配.md` §2 | 团队开发基准，所有成员对齐理解 |
| 接口清单 | `团队任务分配.md` §7 | 前后端接口约定 |
| WebSocket 格式 | `团队任务分配.md` §6 | 实时通信协议约定 |
| 三周冲刺计划 | `团队任务分配.md` §5 | 含验收标准的可执行目标 |

---

## 5. 后续行动项

| 优先级 | 行动 | 负责人 |
|--------|------|--------|
| P0 | 成员 B 实现 CorsConfig（B1） | B |
| P0 | 成员 A 实现 ConversationService + MessageService（A1-A2） | A |
| P0 | 成员 C 改造 ChatList 展示会话（C8） | C |
| P1 | 成员 A 实现 WebSocketController（A7） | A |
| P1 | 成员 D 实现 Claude 适配器（D1） | D |
| P1 | 成员 C 提取 MessageInput 组件（C6） | C |
| P2 | 成员 C 实现 OfficeView 底部面板（C10） | C |
| P2 | 成员 A 新增 ConversationMember 实体 | A |
