# 开发过程记录 — 前端交互体验优化与办公室逻辑完善

> **日期**: 2026-06-10\
> **任务编号**: PR#14-review-ux\
> **所属模块**: frontend / backend-java\
> **前置依赖**: PR#14 合并 ✅

---

## 一、背景

PR#14 合并后，项目在测试中暴露出多个影响用户体验的问题：

1. **加载动画异常**：Agent 生成大项目时文本流结束后 `isLoading` 立即关闭，project_bundle 卡片需 1.5s 延迟才到达，中间空白期用户误以为 Agent 已停止
2. **刷新导致流式中断**：流式输出中刷新浏览器 → WebSocket 断连 → 后续 token 丢失，刷新后出现两条相同消息
3. **聊天窗口不自动滚动**：流式 token 追加时窗口停留在原位，用户需手动滚动
4. **办公室与群聊不一致**：办公室创建时无法选择 Agent、成员变更不同步到会话栏、刷新后成员头像丢失、踢人不生效

---

## 二、任务执行过程

### 2.1 加载动画修复 — 延迟关闭 loading

**文件**：`frontend/src/stores/chat.ts`

**问题**：`handleToken` 中 `isComplete=true` 时无条件 `isLoading = false`。

**修复**：文本消息完成时启动 3 秒延迟定时器；若 project_bundle 在延迟内到达则立即关闭 loading。

```typescript
// 文本流结束 → 延迟关闭 loading，给 project_bundle 卡片留到达窗口
if (chunk.messageType !== 'project_bundle') {
  if (loadingDelayTimer) clearTimeout(loadingDelayTimer)
  loadingDelayTimer = setTimeout(() => {
    isLoading.value = false
    loadingDelayTimer = null
  }, 3000)
} else {
  if (loadingDelayTimer) { clearTimeout(loadingDelayTimer); loadingDelayTimer = null }
  isLoading.value = false
}
```

新增 `loadingDelayTimer` 与 `sendTimeout` 并列管理，在 `handleWsError` 和 `resetState` 中同步清理。

---

### 2.2 流式中断防护

#### 2.2.1 前端 — 刷新前确认

**文件**：`frontend/src/views/ChatView.vue`

```typescript
// beforeunload: 流式进行中阻止刷新/关闭
function handleBeforeUnload(e: BeforeUnloadEvent) {
  if (chatStore.isLoading) e.preventDefault()
}
onMounted(() => window.addEventListener('beforeunload', handleBeforeUnload))

// Vue Router 内部导航也阻止
onBeforeRouteLeave((_to, _from) => {
  if (chatStore.isLoading) {
    return window.confirm('Agent 正在生成回复，确定要离开当前会话吗？')
  }
  return true
})
```

#### 2.2.2 Java — 并发防重复入库

**文件**：`backend-java/.../WebSocketController.java`

新增 `ConcurrentHashMap<String, Boolean> activeConversations` 守卫：

```java
// 防止同一会话并发处理导致消息重复入库
if (activeConversations.putIfAbsent(conversationId, Boolean.TRUE) != null) {
    log.warn("Conversation {} is already being processed, skipping duplicate", conversationId);
    return;
}
```

Agent 完成/出错时 `remove` 释放锁。

#### 2.2.3 前端 — 延迟 token 复用

**文件**：`frontend/src/stores/chat.ts`

刷新后 WebSocket 重连 → 延迟 token 到达 → `handleToken` 创建流式消息前检查最后一条 assistant 消息是否 10s 内创建，若是则复用：

```typescript
const lastMsg = msgs[msgs.length - 1]
if (lastMsg && lastMsg.role === 'assistant' && lastMsg.type === 'text') {
  const age = Date.now() - new Date(lastMsg.created_at).getTime()
  if (age < 10000) {
    streamingMessageId.value = lastMsg.id
    streamMsg = lastMsg
  }
}
```

---

### 2.3 聊天窗口自动滚动

**文件**：`frontend/src/components/chat/ChatWindow.vue`

**根因**：只监听 `currentMessages.length` 变化（新消息时滚动），流式输出是同一消息内容增长 → 不触发。

**修复**：新增 watcher 监听最后一条消息的 `content.length`：

```typescript
watch(
  () => {
    const msgs = currentMessages.value
    return msgs.length > 0 ? msgs[msgs.length - 1].content.length : 0
  },
  () => { scrollToBottom() }
)
```

---

### 2.4 办公室逻辑完善

#### 2.4.1 创建时选择 Agent

**文件**：`frontend/src/components/office/CreateOfficeModal.vue`、`stores/chat.ts`

CreateOfficeModal 新增 Agent 多选区域，`createOffice()` 接受 `agentIds` 参数，传入 `apiCreateConversation` 并同步为办公室初始成员。

#### 2.4.2 成员变更同步后端

**文件**：`frontend/src/stores/chat.ts`

新增 `syncOfficeAgentsToConversation()` 和 `extractAgentIds()`：

```typescript
async function syncOfficeAgentsToConversation() {
  const office = currentOffice.value
  if (!office?.conversationId) return
  const agentIds = extractAgentIds()  // 提取 agent_ 开头的成员ID
  await updateConversationAgents(office.conversationId, { agentIds })
}
```

`inviteMember` / `removeMember` 执行本地变更后调用此函数同步到 Java `PUT /conversations/{id}/agents`。

#### 2.4.3 刷新后成员还原

**文件**：`frontend/src/stores/chat.ts`

`syncOfficesFromConversations()` 重构：
- 新建办公室时从 `conv.agentNames` 匹配 store 中已加载的 agents → 还原成员信息
- 已有办公室但仅 owner（被旧 sync 覆盖）→ 补充同步 Agent 成员

#### 2.4.4 踢人清空修复

**文件**：`backend-java/.../ConversationController.java`

**根因**：`UpdateConversationAgentsRequest.agentIds` 标注 `@NotEmpty`，前端踢光全部成员时发 `agentIds: []` → 400 拒绝 → 成员残留。

**修复**：
1. 移除 `@NotEmpty`，允许空数组
2. `updateAgents()` 改用 `removeAgentFromConversation` + `addAgentToConversation` 逐个操作，每个独立事务保证持久化

#### 2.4.5 初始进入优化

**文件**：`frontend/src/components/office/OfficeView.vue`

- `onMounted` 新增 `loadAgents()` → syncOffices 可按名称匹配 Agent 还原成员
- 无路由指定时自动选中第一个办公室
- 无办公室时显示引导空状态（对齐 ChatWindow 空状态设计）

---

## 三、关键决策记录

| # | 决策 | 结论 | 理由 |
|---|------|------|------|
| 1 | loading 延迟关闭方案 | 3 秒定时器 + project_bundle 到达时取消 | 比 Java 端改简单，不影响现有时序 |
| 2 | 刷新防护策略 | 三层：beforeunload + Java 防并发 + 前端复用 | 单一层不可靠，三层覆盖所有竞态 |
| 3 | 办公室状态同步 | 前端主动调 API + 刷新时从会话列表还原 | 双向同步保证一致性 |
| 4 | 踢人持久化 | 去掉 @NotEmpty + 逐个 remove + add | 每次操作独立事务，空数组也能正确落库 |
| 5 | 空状态设计 | 复用 ChatWindow 空状态样式 | 保持整体 UI 一致性 |

---

## 四、产物清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `frontend/src/stores/chat.ts` | 修改 | loadingDelayTimer、10s 复用、syncOfficeAgentsToConversation、syncOffices 重构、createOffice 扩展 |
| `frontend/src/views/ChatView.vue` | 修改 | beforeunload + onBeforeRouteLeave 守卫 |
| `frontend/src/components/chat/ChatWindow.vue` | 修改 | content.length watcher 自动滚动 |
| `frontend/src/components/office/OfficeView.vue` | 修改 | 空状态+自动选中+loadAgents |
| `frontend/src/components/office/CreateOfficeModal.vue` | 修改 | Agent 多选区域 |
| `backend-java/.../WebSocketController.java` | 修改 | ConcurrentHashMap 防并发 |
| `backend-java/.../ConversationController.java` | 修改 | @NotEmpty 移除 + updateAgents 事务修复 |

---

## 五、提交历史

| 提交 | 内容 |
|------|------|
| `93d5ab7` | fix: 加载动画延迟 3s 关闭 |
| `9b09d51` | fix: 流式期间阻止刷新/导航离开 |
| `1a2795d` | fix: Java 防并发重复入库 |
| `10ead22` | fix: 前端延迟 token 复用 |
| `f940212` | fix: 自动滚动跟随 |
| `553f795` | fix: 办公室创建支持选择 Agent |
| `68eb099` | fix: 成员变更同步后端 |
| `250cbb5` | fix: 刷新后成员头像还原 |
| `77e7d44` | fix: 踢人清空 @NotEmpty 修复 |
| `f7147d3` | feat: 办公室初始进入优化 |
