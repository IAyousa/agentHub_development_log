# 开发过程记录 — C10 办公室底部聊天抽屉实现

> **日期**: 2026-06-06\
> **关联分支**: `dev`\
> **关联 commit**: `30f3c49`\
> **涉及范围**: OfficeView 聊天抽屉、办公室与群组会话绑定、路由参数、刷新恢复

---

## 一、背景

C10 任务定义：在 OfficeView（SVG 办公室场景）底部新增可折叠/展开的聊天抽屉，嵌入共享的 ChatMessage + MessageInput 组件。该任务属于 P2（第三周+），纯前端开发，不依赖 Java/Python 后端改动。

### 1.1 现有状态

OfficeView 已有以下功能：
- SVG 办公室场景（8 个座位、3D 桌子、绿植装饰）
- GSAP 人物动画（走路、踢人）
- 创建/解散办公室弹窗
- 成员邀请/移除面板
- 办公室切换选项卡

但完全没有聊天功能。用户需要在办公室场景中也能与其他成员交流。

### 1.2 设计依据

团队任务分配文档 §2.3 定义了方式 2："底部抽屉式布局，保留 SVG 办公室场景的沉浸感"。聊天面板可折叠/展开，复用 ChatMessage + MessageInput，不写第二遍消息逻辑。

---

## 二、方案设计

### 2.1 架构

```
OfficeView
├── Header（办公室标题 + 选项卡）
├── SVG Scene（办公室场景）
│   └── 右下角悬浮聊天按钮（toggle）
└── Chat Drawer（底部抽屉，Transition 动画）
    ├── Header（标题 + 关闭按钮）
    ├── Messages（ChatMessage × N）
    ├── Loading Indicator
    └── MessageInput
```

聊天数据流复用现有 Pinia store 的 `currentMessages`、`sendMessage()`、`isLoading`。

### 2.2 关键决策：办公室与群组会话绑定

办公室需要一个 `conversationId` 来关联专属的聊天会话。创建办公室时调用 `POST /conversations { type: 'group' }` 创建真实会话，返回的 UUID 存为 `office.conversationId`。

---

## 三、实施与问题排查

### 3.1 第一版：基础聊天抽屉

在模板中添加聊天抽屉 HTML 结构，脚本中加 `isChatOpen`、`toggleChat`、`handleOfficeSend`。抽屉使用 Vue `<Transition name="drawer">` 实现从底部滑入的动画效果。

复用 ChatMessage + MessageInput 组件，连接 `chatStore.sendMessage()`。

**编译通过，基础功能可用。**

### 3.2 问题 1：ChatList 点击群组无法跳转

**用户反馈**："在 ChatView 点击测试办公室的会话栏时，没有自动跳转到对应办公室"

**分析**：
1. ChatList 的 `handleSelect` 对 `type === 'group'` 使用 `router.push('/office/' + id)`，但路由没有 `/office/:id` 参数
2. Office 和 Conversation 之间缺少反向查找——office 有 `conversationId`，但从 conversation 找不到对应的 office

**修复**：
- 路由加可选参数：`/office/:conversationId?`
- OfficeView `onMounted` 读取 `route.params.conversationId`，查找匹配的办公室并自动打开聊天

### 3.3 问题 2：Pinia `storeToRefs` 崩溃

**用户反馈**："前端页面无法正常加载，控制台 `Cannot read properties of null (reading 'effect')`"

**分析**：使用了 `let pendingOfficeChatId: string | null = null`（普通变量）作为 store 间传递数据的桥接变量。Pinia 的 `storeToRefs` 在处理非 ref 属性时可能出现兼容性问题。

**修复**：放弃桥接变量方案，改用路由参数传递 `conversationId`。架构更干净，也符合 REST 惯例。

### 3.4 问题 3：刷新后办公室消失

**用户反馈**："一刷新浏览器，新创建的办公室就消失了，但会话栏的会话还留着，且点击跳转的不是对应的办公室"

**分析**：办公室数据只存在 Pinia store 内存中，刷新即丢失。而群组会话存在后端数据库，刷新后仍可见。

**修复**：新增 `syncOfficesFromConversations()`：
- 扫描 `conversationList` 中的 group 类型会话
- 为每个没有对应办公室的群组会话自动创建虚拟办公室
- 刷新后办公室从群组会话数据中重建

### 3.5 问题 4：创建后聊天抽屉不更新

**用户反馈**："在创建后打开的新办公室页面中，对话栏还是创建前所停留的办公室的"

**分析**：多个交互场景的叠加冲突：
1. `onMounted` 中路由参数触发 `isChatOpen = true`，抽屉自动打开
2. `handleCreateOffice` 是异步的（`apiCreateConversation`），未 await
3. 创建完成后抽屉一直开着，显示的是 `onMounted` 时绑定的旧办公室

**修复链**：
- `handleCreateOffice` 改为 `async/await`
- 创建后 `isChatOpen = false`（符合用户期望：创建完不自动弹聊天）

### 3.6 问题 5：WebSocket 未初始化

**用户反馈**："创建后第一时间不能绑定到对应的会话中，得切换页面或刷新浏览器才可以"

**分析**：`initWebSocket()` 只在 ChatView 的 `onMounted` 中调用。如果用户直接进入 `/office` 页面（从未访问 ChatView），WebSocket 连接从未建立。

**修复**：OfficeView 的 `onMounted` 中加 `chatStore.initWebSocket()`。

### 3.7 问题 6：删除群组会话后办公室残留

**用户反馈**："在 ChatView 栏中把办公室的会话删除后，进入办公室界面仍可看见已删除的办公室选项卡"

**分析**：`syncOfficesFromConversations` 只有新增逻辑（ADD），没有清理逻辑（REMOVE）。

**修复**：扩展为双向同步：
```ts
// 移除 conversationList 中已不存在的群组会话对应的办公室
const existingConvIds = new Set(conversationList.value.filter(c => c.type === 'group').map(c => c.id))
offices.value = offices.value.filter(o => !o.conversationId || existingConvIds.has(o.conversationId))
```

---

## 四、最终产物

### 4.1 代码变更

| 文件 | 改动 | 说明 |
|------|------|------|
| `OfficeView.vue` | +155 行 | 聊天抽屉模板 + JS 逻辑 + CSS 动画 |
| `chat.ts` | +47 行 | Office.conversationId、createOffice 调 API、syncOfficesFromConversations、openOfficeChat |
| `router/index.ts` | +1 行 | `/office/:conversationId?` 可选参数 |

### 4.2 提交记录

```
30f3c49 feat(frontend): 实现C10办公室底部聊天抽屉，绑定真实会话+刷新恢复
```

### 4.3 功能清单

| 功能 | 状态 |
|------|:--:|
| 底部抽屉聊天面板（可折叠/展开） | ✅ |
| 创建办公室自动创建群组会话 | ✅ |
| ChatList 点击群组跳转到对应办公室 | ✅ |
| 刷新后办公室从群组会话恢复 | ✅ |
| 删除群组会话自动移除对应办公室 | ✅ |
| WebSocket 在 OfficeView 初始化 | ✅ |
| 创建办公室后抽屉默认关闭 | ✅ |

---

## 五、经验总结

### 5.1 数据持久化要尽早考虑

办公室数据最初只在 Pinia 内存中，刷新即消失。在开发过程中才意识到需要从后端恢复数据。如果一开始就设计数据模型时考虑持久化（办公室 ↔ 群组会话的映射关系），开发流程会更顺畅。

### 5.2 路由参数是清晰的跨页面通信方式

最初尝试用 Pinia store 的桥接变量（`pendingOfficeChatId`）在 ChatList 和 OfficeView 之间传递数据，但遇到了 `storeToRefs` 兼容性和非响应式数据的生命周期问题。改用路由参数后，架构更清晰，也更符合 Vue Router 的设计理念。

### 5.3 前端组件复用经验很重要

C10 实现中最有价值的设计决策是复用 ChatMessage + MessageInput 组件。办公室聊天抽屉只需 155 行代码（模板+JS+CSS），核心聊天功能完全由已有组件提供。这符合任务文档"不写两遍消息逻辑"的设计原则。

### 5.4 异步操作的 UI 反馈需要主动管理

`createOffice` 改为异步调用 API 后，出现了多个时序相关的 UI 问题（抽屉不更新、状态不切换）。异步操作的生命周期管理——加载中、完成、错误——每一步都需要对应的 UI 状态处理。
